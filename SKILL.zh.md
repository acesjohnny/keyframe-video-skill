---
name: keyframe-video-production
description: 用于制作以关键帧驱动的短视频——设计分镜、生成关键帧图片、临时公开托管图片、提交视频生成任务、合成成片。涵盖人物对白、跨镜头角色连续性，以及在每个不可逆或产生费用的步骤前停下来等人确认。
---

# 关键帧视频制作

把一个场景变成多镜头短视频，其中每个镜头都从一张生成的关键帧图片开始。

本工作流适用于任何关键帧驱动的视频模型。它是基于 Agnes 视频 API（`agnes-video-2.5-flash`）编写并验证的——该接口接收一个 `first_frame` 图片 URL 加一段提示词，返回一个 MP4。

## 适用场景

用它来做：

- 短 demo（典型是 3 个镜头 × 5 秒），不是成品影片。
- 镜头必须从指定图片起始，因此图片需要能通过 HTTPS 公开取到。
- 跨镜头的角色或场景连续性很重要。
- 用户希望**在花钱之前**审方案，而不是拿到成品再来评价。

**不要**用它做：无关键帧的纯文生视频、长片剪辑，或任何未经重新确认就对外发布的东西。

## 实测结论

在 `agnes-video-2.5-flash` 的 keyframe 模式下实测，2026 年 9 月：

- 它**确实能**直接从关键帧加一段含台词的提示词，生成可听懂的中文普通话语音，并带匹配的口型。无需外挂 TTS。这个判定来自人工收听，不是自动分析。
- 返回的片段带真实环境音，不是静音轨。
- 生成的语音偏轻，均值约 -35 dBFS，要预留响度归一化这一步。
- 一把视频模型的 Key 上**可能完全没有语音/TTS 模型**。承诺 TTS 兜底之前，先列一下可用模型。

以上都是**对某一家供应商在某个时间点的观察**，不是保证。依赖它之前请重新验证。

## 硬性规则

1. **四道闸，四次停。** 剧本对白分镜 → 图片提示词 → 出图 → 托管。每一道都停下等用户。不要因为上一次的回答听起来很积极就一路跑到底。
2. **托管是独立的批准项。** 把图片传到公开 URL 是部署动作，与「批准这些图片」是两回事，要单独问。
3. **批准不会自动扩大。** 一句「可以」只授权你刚才描述的动作。重跑、多加一个镜头、多一个计费任务，都要重新问。
4. **核实素材来源。** 抓取工具给的摘要可能连「这是第几章」都搞错。在此之上做任何设计之前，先读原始内容。
5. **对白必须原创。** 借场景结构，不借句子。不要在提示词或产物中复述原作的文字。
6. **Agent 听不见。** 音频包络和口型分析能证明「有人在说话」，但**永远不能**证明语种或台词内容正确。那个判定属于人类；在人给出结论之前，结果一律记为「未验证」。
7. **没查过的账单不要当事实说。** 明确讲这是口径假设。

## 工作流

### 1. 素材与分镜（第一道闸）

抓取真实素材文本，并从原始内容而非摘要核实它的身份。挑一个时长合适的桥段；如果是对白测试，挑台词短促有力的那种。

给出分镜表：镜头、时长、画面、台词、说话人。明确说明台词是原创的。**在最开始**就问清楚画风方向和语音路线——这两项会改变后面每一条提示词。

### 2. 图片提示词（第二道闸）

写一份**角色设定（character bible）**——一整块文字，定义所有反复出现的角色、场景和负向约束——并在每条镜头提示词中**逐字复用**。这才是维系连续性的东西；各镜头自己的文字只补充景别和动作。

把提示词写进文件，不要塞进命令行参数。长的非 ASCII 文本会被 shell 转义弄坏。

做口型测试时，每条提示词都要求脸部完整可见、嘴部无遮挡。一个构图优美但说话人是侧脸的镜头，什么都测不出来。

出图之前先把提示词交给用户审。

### 3. 出图与审图（第三道闸）

生成关键帧后，**先自己看一遍**再拿给用户。检查：跨镜头的角色一致性、脸和嘴是否可见、景别是否有变化（几个镜头构图雷同会让成片显得呆板）、以及任何风格化的生物或面孔是否掉进了恐怖谷。

把不好的地方讲得和好的地方一样清楚，然后让用户决定。返工时把被替换的版本归档，而不是直接覆盖；同时收紧角色设定，让这次的修正在所有重出的镜头上都成立。

关键帧应该表现 t=0 的状态，即动作发生**之前**——运动是视频要补上的东西。如果图片已经画出了结果状态，那个节拍就没了。

### 4. 托管（第四道闸）

keyframe 模式需要每张图一个公开的 HTTPS URL。Firebase Hosting 的预览频道很适合，因为它自带过期机制。用一个**新建的、专用的**短期频道，不要复用上一轮的——之前的任务记录里还引用着那些 URL，覆盖它们等于毁掉证据链。

托管之前，先把上传目录搭好并扫描：API 密钥字符串、通用凭证特征、以及图片元数据里的路径、提示词原文和身份信息。某些生成器的图片会嵌入 C2PA 来源清单和不可见水印——内部测试无妨，但要说明，且在把这类图用于公开交付物之前想清楚。

事先讲明：目标、过期时间、精确的文件清单（含字节数与哈希）、执行命令、一份**不会**被触碰的东西的清单（生产站点、其他频道、数据库规则、认证设置、计费套餐）、如何验证、以及如何回滚。

托管之后，匿名 GET 每一个 URL，把 SHA-256 与本地比对，再重新列出托管目标，证明生产环境和之前的频道都没被动过。结果存成 JSON。

### 5. 提交与合成

每个镜头提交一个任务。在 POST **之前**就把任务记录落盘，这样中断的提交可以恢复，且绝不会静默重发。用返回的 video id 轮询。

合成时先做归一化（统一缩放、帧率、像素格式；静音轨补齐；响度归一化），再做拼接。从成片里抽帧，然后自己看。

### 6. 记录

写下产出了什么，并把**未验证**的事项——语种判定、账单、任何异常——单独列一节，而不是埋在正文里。等用户后来给出判定，再回写，并写明是谁判的。

## 轮次隔离

第二轮不得覆盖第一轮。把路径写死成单一 `output/` 目录的脚本，会清掉之前的片段、任务记录和成片。给每一轮自己的子目录和自己的镜头定义，并验证「不带参数」时的行为与第一轮完全一致。

## 验收清单

- 素材身份已从原始内容核实，不是摘要。
- 角色设定在所有镜头提示词中逐字一致。
- 上传目录只含预期文件；凭证与元数据扫描干净。
- 每张托管的图片：HTTP 200、内容类型正确、SHA-256 与本地相符。
- 生产环境与既有托管目标未被改动。
- 任务数在授权范围内。
- 成片：时长、分辨率、帧率符合预期，音轨存在。
- 抽帧已用眼睛看过。
- 未验证的结论已记为未验证。

## 常见故障

### 任务完成了却什么都没下载下来

客户端去 `metadata.url` 找素材地址，而接口把 `url` 放在响应顶层，于是成功路径反而抛错、没有保存。两个都接受：

```python
url = result.get('url') or (result.get('metadata') or {}).get('url')
```

### 生成用的 CLI 拒绝它自己的默认模型

服务端的默认模型会跑在已安装的 CLI 版本前面，报错表现为一个硬性 400，让你升级。在动手全量升级 CLI 之前，先显式钉一个较旧的模型；这类 CLI 通常在本地缓存了可用模型名单。

### 字幕被烧进画面

一旦要求对白，模型就会自作主张加字幕。每条提示词都必须明确禁止文字、字幕和水印。

### 成片里对白太轻

生成的语音可能低到 -35 dBFS 附近。在逐片段的归一化环节做响度归一化可以提上来。之后要检查成片的峰值电平，因为拼接时的重编码可能把峰值推到满刻度而削波。

### 多出来或少掉的话

一个镜头的音频里可能出现比脚本更多的发声段。数一数包络里的爆发次数，把差异指出来，让人收听后判断。

### 在听不见的情况下证明「有语音」

你无法直接验证音频，但可以拿到真实证据。计算每 100 毫秒的 RMS 包络：低底噪之上出现离散爆发、动态范围大致超过 20 dB，指向语音；纯环境音通常在 12 dB 以内。然后在这些爆发的时间点抽帧，检查口型在帧之间是否真的变化。把这个当作**有语音**的证据来报告，绝不当作语种或内容正确的确认。

### macOS 上没有 `timeout`

macOS 不是 coreutils 环境。用你自己 harness 的调用超时，而不是去包裹命令。

### 残留的 `.git/index.lock`

一次被中断的 git 写操作留下的 0 字节锁文件，会静默地挡住之后**所有**的 git 操作，只要它还在。确认它是 0 字节、时间戳很旧、且没有 `git` 进程在跑，然后删掉它。

### 视频文件进不了 git

很多仓库刻意排除 `*.mp4` 之类来阻止仓库膨胀，而这种排除很容易被误认为是疏漏。先弄清楚这条规则是不是有意为之，把冲突讲出来，而不是强行 add。大媒体通常该进备份或对象存储，不该进 git 历史——历史事后削不掉，除非重写。
