---
name: keyframe-video-production
description: 用于制作以关键帧驱动的短视频——设计分镜、生成关键帧图片、临时公开托管图片、提交视频生成任务、合成成片。涵盖人物对白、跨镜头角色连续性，以及在每个不可逆或产生费用的步骤前停下来等人确认。
---

# 关键帧视频制作

把一个场景变成多镜头短视频，其中每个镜头都从一张生成的关键帧图片开始。

本工作流适用于任何关键帧驱动的视频模型。它是基于 Agnes 视频 API（`agnes-video-2.5-flash`）编写并验证的——该接口接收一个 `first_frame` 图片 URL 加一段提示词，返回一个 MP4。

## 适用场景

用它来做：

- 目标是多镜头视频——短 demo、一集，或整部改编作品——每个镜头都从一张指定的关键帧起始。
- 视频接口要求这张图能通过 HTTPS 公开取到，或接受内联（见第四道闸）。
- 跨镜头的角色或场景连续性很重要。
- 用户希望在花钱之前审方案，而不是拿到成品再来评价。

**不要**用它做：无关键帧的纯文生视频、剪辑现成的素材，或任何未经重新确认就对外发布的东西。

**任何 agent，任何关键帧驱动的模型。** 这里没有任何一条依赖某一个 agent 或某一家供应商。agent 需要能读文件、跑 shell 命令、发 HTTP 请求——Claude Code、Codex 和 Hermes Agent 都符合。视频模型需要有关键帧（首帧）模式。本文是基于 `agnes-video-2.5-flash` 编写并验证的，关键帧来自一个 OpenAI 图像模型和 `agnes-image-2.5-flash`；其他图像与视频模型（包括 MiniMax 的）套用同一套闸门，各自的限制要另行核查。

## 实测结论

在 `agnes-video-2.5-flash` 的 keyframe 模式下实测，2026 年 9 月：

- 它**确实能**直接从关键帧加一段含台词的提示词，生成可听懂的中文普通话语音，并带匹配的口型。无需外挂 TTS。这个判定来自人工收听，不是自动分析。已确认四次——最近一次是提示词语种 A/B（2026-09-10），两个版本都说的中文，差别在质量而不在语种；还有一部 18 镜的片子（2026-09-10），它同时回答了另一个问题：**两个不同的声音能在同一部片子里各自站住**——一个少女和一个小孩，各有自己的声音设定，十三个说话镜头里始终没串。
- **可以附一段声音样本，Agnes 会接受**（2026-09-21，`agnes-video-2.5-flash`）：在 `reference` 模式的 `audios` 里放一段角色的人声，提示词里用 `<Audio 1>` 指代。任务被接受并完成；目前唯一一组 A/B 里，听的人判定声音比不挂样本时更像样本，新台词里也没漏出样本原词。代价是首帧——reference 模式不收首帧。做法见下文「声音样本，或声音描述」。
- **没有任何开关能关掉生成的音轨**，也没有负向提示词字段——这是对照供应商自己的 API 参考文档查的（2026-09-11），不是看摘要。也不能一边保留已通过的首帧、一边塞给它一条干净的音轨：keyframe 模式拒收 `audios`，而收 `audios` 的 reference 模式拒收 `first_frame`，关键帧就从「这是第一帧」降级成「拿它当风格参考」。所以所有音频问题都只能在提示词里或合成阶段解决。
- 画外音旁白**探针能过、上生产就翻车**，别指望它。一次受控的单镜探针（2026-09-09）出来的旁白清晰、画面里所有的嘴都闭着；同一手法放到一部成片的十个镜头里，好几镜的角色都在对着旁白对口型。提示词可以写*没人*说话，但只要有音频、画面里又有脸，模型就倾向于给它找一张嘴。改成给旁白一个**看得见的说话人**——把讲故事的人放进画面、站在一侧，故事在他身后展开。这就把一条负向约束（「谁都不许对口型」）变成了模型本来就稳的正向约束（「这个人在说话，其他人都不出声」）。**已验证**于一次十镜重做（2026-09-09）：每镜抽六个时间点，讲述者都在对口型，他身后的每个角色都闭着嘴，无一例外。另一条路——现在是讲解片的默认路线——是把旁白从视频模型手里整个拿走：镜头按*无对白*片段生成，合成时把单独合成的旁白垫在下面（见*无主讲人的旁白片*）。「画面里的人在旁白期间不说话」这种提示词只在那条路上才安全——一旦要视频模型自己对着一张脸产出旁白音频，就绝不能这么写。**画面里没有脸**的画外音镜（一件道具、一张桌子、一片空景）是视频模型仍可以自己念台词的唯一情形：用 `reference` 模式提交，挂本片一个已通过片段的声音样本，这样整集画外音的声线就钉住了（用户的规矩，2026-09-22，人耳验证）；画面里有脸时，同样的 reference 提交让那个人对着旁白开口了，所以有脸的一律走 TTS。
- **价格，2026-09-11 在供应商价格页核对：**`agnes-video-2.5-flash` 和 `agnes-image-*-flash` 系列模型标为限时 $0，而 `agnes-video-2.5`（不带 Flash）按秒计费。优惠会结束；引用之前再查一遍价格页。
- 返回的片段带真实环境音，不是静音轨。
- 生成的语音偏轻，均值约 -35 dBFS，要预留响度归一化这一步。
- 配音是按片段分配的，没有可以钉住的 id，所以除非提示词描述了声音本身，同一个角色的音色会在一部片子里漂。写一段**声音设定（voice bible）**——年龄、性别、音色、音域、语速、口音、表达方式——并在该角色说话的每个片段里逐字重复，就像人物设定固定外貌那样。并且不要写逐镜的语气指示：一镜「温暖亲切」、一镜「揶揄」、一镜「加重语气」，这是模型据以换人的变量，也是它会烧进字幕的文字。**已验证** 2026-09-09：同一角色的九个片段，先前音色乱漂，加了这段、去掉逐镜语气行之后回来成了一个声音。判定来自人工收听，不是分析——agent 听不见。它能收窄漂移，但钉不住声音：后来的一部片子（2026-09-21）里，一位带完整声音设定的出镜主讲人，六个片段还是回来了六个人（见*为什么放弃出镜主讲人*）。要整部片子一个声音，就得规划 `reference` 模式挂声音样本，或者旁白走 TTS，把声音设定当底线，不当解药。
- **片段会自己在内部切镜。**只要一个镜头，模型有时会在一个片段里回两三个镜头——反打、换装、陌生人走进来、换了一栋楼——于是 5 秒的请求只得到 2 秒能用的。一轮 18 个片段（2026-09-14）里七个都这样。在视频提示词最上方写上 **"one single unbroken take from one locked camera position; the location, angle and framing stay exactly as in the first frame from the first frame to the last"** 就止住了：接下来的十五个任务（2026-09-15）**已验证**零内部切镜，其中包括一个移动的主观视角行走和一个缓慢推进。要检测它，别信它——见下文*模型在片段内部切镜*。
- **但你编排好的切镜是能力，不是缺陷。**「一个不间断长镜头」那句治的是*没编排*的切镜。明确要它在一个片段里按时长拍三个镜头，同一个模型能准时交付：3 秒 + 6 秒 + 3 秒的街头戏（2026-09-21，`agnes-video-2.5-flash`，**keyframe** 模式）在 3.08 秒和 9.92 秒切——恰好两刀，没有多余的——每句台词落在自己那一镜里。建在同一批模型上的一个供应商网页端日常就这么干：它的三条片段分别在 3.04/9.00、3.00/6.21/8.04 切，第三条也按比例切。单镜头照旧用长镜头句；编排段落把它换掉，两者绝不能同用。见下文*编排式多镜头段落*。
- 一把视频模型的 API key 上**可能完全没有语音或 TTS 模型**。承诺 TTS 兜底之前，先列一下可用模型——在假定供应商能承接哪个*阶段*之前，再列一遍。跑视频的这把 key 可能同时带着图像模型，这种情况下为了出关键帧去用一个不相干的 CLI，换来的是第二份额度、第二条流水线，以及一个谁都验证不了的模型 id。
- 一个生成 CLI 的图像工具在 CLI 层面可能**没有模型选择器、也没有可验证的模型 id**。但产物本身可能还带着一个：C2PA 清单（PNG 的 `caBX` 块）可以写明生成器和版本。宣布模型不可知之前先读它——但仍然要按「元数据声称」来报告，而不是当成你验证过的版本，因为清单里的版本字段未必对得上供应商的市场名称。

以上都是对某一家供应商在某个时间点的观察，不是保证。依赖它之前请重新验证。

**换第二家图像供应商不是即插即用。**把关键帧生成器换成 MiniMax `image-01`（2026-09-16，一张付费图）一次尝试就撞出三条限制，换任何供应商都适用：

- **Key 分区域。**同一把 key 在 `api.minimaxi.com` 有效、在 `api.minimax.io` 被拒。用一个不会产出结果的请求去探——发一个非法参数、读错误信息——而不是烧一次真实生成来查 key 属于哪个主机。
- **提示词长度是硬上限，截断是无声的。**上限 1500 字符，对上一条 4136 字符的关键帧提示词；框架直接截掉照样生成。模型有上限时，每个任务备一条短版；没有短版时在方案里预警。
- **只收一张参考图，不收一组。**我们的关键帧靠人物卡加场景卡加道具卡；`subject_reference` 只收一张。结果既不像已通过的脸也不像场景，提示词里的珍珠根本没出现。跨镜头的角色一致性正是多张参考图买来的东西，所以只收一张参考的供应商适合出单张图，不适合有连续性约束的片子。

## 硬性规则

1. **每道闸都是一次停。**剧本/对白/分镜，然后走位，然后图片提示词，然后人物设定卡，然后出图，然后托管。每一道都停下等用户。不要因为上一次的回答听起来很积极就一路跑到底。闸门数随着工作流一起在涨；不变的是，过了一道闸绝不等于过了下一道。
2. **托管是独立的批准项。**把图片传到公开 URL 是部署动作，与「批准这些图片」是两回事，要单独问。
3. **批准不会自动扩大。**一句「可以」只授权你刚才描述的动作。重跑、多加一个镜头、多一个计费任务，都要重新问。
4. **核实素材来源。**抓取工具给的摘要可能连「这是第几章」都搞错。在此之上做任何设计之前，先读原始内容。
5. **对白必须原创。**借场景结构，不借句子。不要在提示词或产物中复述原作的文字。
6. **Agent 听不见。**音频包络和口型分析能证明*有人在说话*，但永远不能证明语种或台词内容正确。那个判定属于人类；在人给出结论之前，结果一律记为「未验证」。
7. **没查过的账单不要当事实说。**明确讲这是假设，而且要说出口。

## 工作流

### 开跑之前：先定在哪边操作

开头问一次，把答案记在项目页上。同一条流水线有三种跑法，中途也可以换：

| 方式 | 谁来干活 | 另一边做什么 |
|---|---|---|
| **agent** | agent 写分镜、走位、提示词和任务文件，用自己的脚本出图、托管、提交。 | 控制台不动；之后打开项目照样看得到全部内容。 |
| **控制台** | 人在画布里操作：写镜头、出卡和关键帧、审批、提交、剪辑。 | agent 出主意、审回来的结果，按要求改文件。 |
| **两者结合** | agent 负责写和查（分镜、走位、提示词、卡、自查）；人在画布里审、批、看进度。每个阶段写明由哪一边提交。 | —— |

两边能互通，是因为**文件才是产品**（见「从本地控制台（画布）驱动」）：双方读写的是同一套任务文件、生成图、审批和任务记录，所以「同步」就是另一边重新读一遍，从来不用搬运。防止两边撞车的规矩：

- **同一条通道同一时间只有一个调度者**，范围是账户下所有项目——免费档按账户限流，不按项目。agent 这边提交前，先确认控制台在*任何*项目的这条通道上都没有排队或在跑的任务。
- **交接要等够间隔。**探针提交了一个任务、把剩下的交给另一边，4 秒后就吃了 HTTP 429。先等满两次提交之间的间隔，或者让一边包办全部发送。
- **控制台当前选中的项目是共享的。**为了看另一个项目去切它，改变了人正在看、而且正在跑视频轮次的那个项目。要看就直接读文件，或者先问。
- **agent 这边的执行器调用供应商的方式要和控制台一模一样**——同样的命令、关闭 stdin、同样的输出路径、任务文件里同样的结果字段——这样任何一边都能接着另一边的活干。
- **把项目交给控制台之前，把被取代的版本标成不通过**并写明原因，让它的审片队列只剩在用的东西。
- **任何项目有任务在跑时，都不要重启控制台**；重启会把在跑的任务标成中断。
- **人说一声就可以换方式。**「先别动画布」和「现在同步过去」都是正常指令，有文件在，哪种都不费事。

### 0. 长篇作品的原著分析（第一道闸之前）

单场戏只需要第一道闸里的素材核对。整部小说则要先做一遍前期分析，它的产出是后面每一道闸的依据：谁出现在书的哪一部分、每个人在每个阶段长什么样、哪些章节归到哪一集。

- **工作副本，不是归档。**抓取原文只为阅读。放在临时目录里；提交的是分析——摘要、统计、章节引用——绝不提交原文。硬性规则 5 对笔记和对提示词同样适用。
- **先量再规划。**总长度、每章、每卷，还有**缺口**：一部 509,000 字的小说（2026-09-11）在来源处就少了两章，不查的话会有两集的剧情留着窟窿。分集之前就把它们找出来，别等分完再发现。
- **人物靠数，不靠记。**按每章提及次数建名单，合并别名（长名优先，免得「姓＋名」又被当成「名」再数一遍），含糊的称呼不算。凭记忆列的种子名单里带了三个来自影视改编、在原著里出现零次的名字。数出来才发现；凭记忆搭名单会给不存在的人建卡。
- **发现用的启发式规则要先验证再信。**用「X 说」这类模式挖说话人，返回的是「忽然开口」这种碎片——原文的标点大多被剥掉了。按检测器规则，弃用。
- **按人物读描写，不是读全书。**把人物名字附近提到年龄、头发、衣着、伤疤或印记的段落抽出来。关键词窗口放得太松，主角一个人就抽出全书的 26%；把关键词限制在名字前后约 25 字以内并去掉常用词，降到 5%，一次能读完。
- **角色设定按阶段写，带证据。**长篇里主角可能换过八种截然不同的样子。给每个阶段写年龄、外貌、服装、印记和性情，每一行标明是原文写明还是推断，并注明章节。追踪全书里状态会变的道具（送出的信物、摔坏、归还），因为拿错阶段的卡就是连贯性错误，什么提示词都救不回来。
- **卡按首次登场排期。**把每个人物首次出现的章节对到集数，按这个顺序建卡；长篇里一半的人物可能到后半部才登场。
- **按章节边界分集。**给每集定一个原文长度目标，绝不跨卷，短尾并入上一集。然后把制作规模说出声：按每镜约七秒，一集 25 分钟大约 220 个镜头。
- **写下来的数字要算出来，绝不估。**一份名单里有两个集数是估着写的（「大约第 18 集」），其中一个整整错了一集；生成那张表的工具本可以打印出精确值。规划文档里一个看着合理的数字，会被原样抄下去没人质疑。

### 1. 素材与分镜（第一道闸）

抓取真实素材文本，并从原始内容而非摘要核实它的身份。挑一个时长合适的桥段；如果是对白测试，挑台词短促有力的那种。

给出分镜表：镜头、时长、画面、台词、说话人。明确说明台词是原创的。**在最开始**就问清楚画风方向和语音路线——这两项会改变后面每一条提示词。

#### 叙事形状与镜头配比：拍给人看的片子（来自一部参考片，2026-09-23）

能力测试只需要一个桥段；拍给观众看的片子需要一个形状，而这个形状可以顺着模型的长处来排。以下几点来自拆解一部传播很广的 AI 短片（13 分半，检出约 147 个切点，镜头中位 4 秒）——只是一个样本，不是实测规律，比例只当起点。

排分镜表之前，先写下两样东西，在第一道闸一起给人看：

- **一个贯穿全片的物件或意象**，每一段都回到它（参考片里是米：井里出米、捡一粒米被当贼、漏米的缸、最后一句「吃饭了」）。碰不到它的镜头，要有存在的理由。
- **一个现实落点**做结尾——一个习俗、一句俗语、观众今天还看得到的东西——让片子停在「原来是这么来的」。这是观众转发的理由。

然后拿下面几条对照分镜表，它们同时也绕开了模型的弱项：

- **大场面前置。** 人海、神兽、火海都在前约 17% 的时长里，之后几乎没有。开头负责留人，后面靠台词和脸。
- **对话用单人近景，1–3 秒一切，一镜一句短台词。** 这是关键帧模型最稳的镜头，镜头短口型也短。与 1.5 的「先说后看」是同一套语法。模型单片段最短 4 秒，这个节奏要靠把切点编排在*一个片段里面*（见「编排式多镜头段落」）来实现，不能靠把整段片段剪短——剪短的片段看着碎（31 镜试点）。
- **大量道具特写和空镜**——手、米袋、雪里的脚、倒影、落进雪里的果子。它们承载情绪，也掩盖角色镜头之间的连贯性漂移。
- **动作戏压到最少。** 参考片唯一的冲突只给了神兽两个短镜，接触的瞬间用雾遮住。
- **主角三个，每人一个缩略图尺寸也看得出的标志**（白须白袍；满脸泥点；白发带）。把标志写进人物卡（2.5）。
- **颜色即语义。** 一套冷色基调；暖色只留给一个意思（火、食物），结尾才允许整体转暖。把规则写进每条关键帧提示词的风格段。
- **书法大字章节卡**叠在空镜上，把长片分成几段，也让段与段之间的画风跳跃显得是有意的。
- **每一到两分钟一个情绪点。** 长不是问题，一长段没有新东西才是。

自己拆参考片的做法：用 ffmpeg 的 `select='gt(scene,0.3)',showinfo` 找切点（要用默认日志级别——`-v error` 会把 showinfo 的输出吞掉），每个镜头中点抽一帧拼成带编号的缩略图拼版；烧进画面的字幕，裁出字幕带做 OCR。对烧了字幕的片子，OCR 比语音识别准，也不需要语音模型。

### 1.5. 走位：写任何镜头之前，先把片子放进一个空间里（第 1.5 道闸）

镜头如果各自写成一段独立的文字，彼此之间就没有共同的几何关系，而提示词表达不出来的连贯性失误全都由此而来：出场名单漏掉了摄影机所跟的那个人，十个镜头到头来是同一个镜头换了皮，一个道具在两刀之间跑到了房间的另一侧。

所以在第二道闸之前，先把每个**场景**描述一遍——不是每个镜头。给每个演员和每台摄影机一个位置、一个朝向和一个视野范围，然后让每个镜头引用一台摄影机，而不是再造一遍几何关系。有三件事就会机械地成立，不用靠小心：

- **镜头的出场名单是算出来的，不是记出来的。**落在摄影机锥形范围内的人就在画面里，包括摄影机所跟的那个人。这是手写时一再出错的那份名单。
- **机位变化在花任何钱之前就能查。**给每个镜头打一个根结构签名再比对；签名相同就是镜头相同，该改的是换一台摄影机，不是换背景。
- **修改是挪一台摄影机或一个演员，不是改一段文字。**改一个人站的位置，会更新所有看得到他的镜头，而不是去十八个文件里改散文然后祈祷。

按片子需要的程度做，能多轻就多轻——一张坐标表加一张俯视图就够；三维预演工具是同一个思路只是精度更高，不是另一回事。要紧的是几何关系只存在一份，放在提示词之外。

同一项检查还能抓到单个镜头看不出来的结构性错误。「这是谁谁谁」的段落——一个人物逐个报人名、摄影机逐个切到被报的人——本质是一镜一镜的画外旁白，锥形检查把一集 20 镜（2026-09-10）里全部五个这样的镜头都标了出来：说话人在自己的摄影机之外。解法是老电影语法**先说后看**：说话人在一个看得见她的镜头里把话说完，下一个没有台词的镜头再给她说的那个人。这样既去掉了「嘴被别人借走」的风险，剪起来也就是更好。

在第 1.5 道闸像展示分镜那样展示这份方案：俯视图、几何关系推出的每镜出场名单、以及所有签名撞车的地方，然后才写第一条提示词。
### 2. 图片提示词（第二道闸）

写一份**角色设定（character bible）**——一整块文字，定义所有反复出现的角色、场景和负向约束——并在每条镜头提示词中**逐字复用**。这才是维系连续性的东西；各镜头自己的文字只补充景别和动作。

**演员表是「谁在画面里」，不是「谁在做动作」。**镜头的演员表决定挂哪几份设定、附哪几张参考图，所以它必须从画面推出来，而不是从动作推出来。一个角色耳朵的特写、耳朵里有一只小生物，这是双角色镜头：只列小生物，那个耳朵的主人就没有设定、没有参考图，她的服装、发型和饰物全凭空重新发明。一部 18 镜影片的 `shot-03`（2026-09-09）就是这么出的事——一只手镯、一条围巾、一枚胸针，设计里根本没有。逐镜过一遍分镜，问每一镜*谁看得见*，包括摄影机贴着的那个人。

**提示词要从素材文件机械拼出来；绝不手工把设定抄进镜头。**十八个镜头逐字节一致，靠自觉做不到。每个块各放一个素材页，用一个小拼装器抽出来，并让拼装器**遇到不存在的块就大声报错**——改名或缺失的块必须是错误，而不是一条悄悄变弱的提示词。这也意味着设计改动只改一处，所有镜头跟着变。

**场景描述用英文写，台词保留它自己的语言。**一次受控 A/B（2026-09-10——同一张关键帧、同一句台词、同样时长，只有提示词语言不同）里，全中文提示词在抽样的全部 24 个时间点都烧出了字幕，其中一处还把它自己的**舞台说明**当成对白渲染出来；英文场景版在同一裁切区检查，任何时间点都没有文字。机理说得通：模型会写它读得懂的剧本，所以提示词里唯一的非英文文字应该就是你真正要它说出口的那句。一个样本是强信号，不是定律——要在真实一轮里重新确认。

**舞台说明不要和台词放在同一段——这条不是建议。**同一次 A/B 里，全中文版不只把舞台说明烧成了字幕，还**把它念了出来**：有人用耳朵确认（2026-09-10），片段说出了提示词自己的指令。凡是挨着对白的东西都可能被当对白执行，所以台词单独成段，关于怎么演的一切都放在别处。

**这条英文规则是在有台词的镜头上测出来的。无台词镜的表现不一样，而且差得很远。**第一轮全英文提示词（2026-09-10，20 镜）里，六个无台词镜全部回来时在说话——其中五个说的是*同一句*中文，一句和故事无关、任何提示词里都没有的体育主持开场白，三个对着镜头说、两个作画外音；第六个在胡言乱语。五张不相干的图片出同一句话，正是模型用默认值填补没人分配的音轨的标志。不依赖具体厂商的解释与音视频联合模型的已记录行为吻合：对白不写，模型就自己即兴一段。英文的无台词条款把禁止的东西点了名（"no narration and no voice-over"），反而更糟，两个没有脸的镜头恰恰出了这个。

试过的版本，都在同一镜、同一张首帧上：

| 版本 | 措辞 | 结果 |
|---|---|---|
| r1 | 英文，"no character speaks / no narration / no voice-over" | 说了主持开场白 |
| r2 | 英文，给配乐一个正向任务，去掉所有与说话有关的词和 *game* 一词 | 嘴反复张开 |
| r3 | r2 逐段照译，**改用中文** | 全程安静；用耳朵确认 |

r3 又带了两镜，但一个面部特写（赌气的男孩）还是说了话，一个远景长出了画外音，还有两个人影朝镜头走来。治好这两镜的是某大厂官方提示词指南里针对这类模型的公式：把声音单独写成一节，拆成**人声／音效／音乐**，把控制句放在人声栏——**「人声：无台词，没有对白或人声」**——再把脸写成全程保持一种表情，而不是写一种有表情的状态（「赌气」会引来嘴部动作）。远景还要用正向措辞把人影钉在远景尺度、远离镜头。

所以无台词镜的做法是：**中文提示词、结构化声音段且人声栏写 无台词、表情固定、不挂与安静相矛盾的角色设定**——设定说某人「正讲到一段停不下来的轶事」而镜头说他一言不发，出来的就是胡言乱语，模型在两头之间取了折中。这条建立在五个镜头上，不是定律；每一镜都要在声音最响的时刻抽帧核查，并把语言判定交给人。脸还是会说话的地方，退路是在关键帧上做本地推镜。

把提示词写进文件，不要塞进命令行参数。长的非 ASCII 文本会被 shell 转义弄坏。

两种提示词语言意味着每个固定块都有两份。长镜头句、画风锁定句和无文字条款各以一个固定英文版本和一个固定中文版本存在于素材块中；拼装器按提示词语言选用哪一份，「逐字节一致」指的是同一语言的提示词之间一致。绝不在镜头提示词里临时翻译某个块。

**故事让两个角色长得一样时，差异要你自己设计——而且要设计两重。**剧情把一个角色变成另一个的复制品，在银幕上是连续性灾难：观众分不清谁在说话。给复制品两个独立的标记，不是一个——一个轮廓差异（头发长短）*加*一个颜色或图案差异——这样即便一个标记因角度、比例或运动模糊丢掉了，两人依然分得开。一部成片（2026-09-10）里两重冗余标记撑住了五个镜头，其中至少两镜若只靠一个标记就会含糊。更好的做法是让某个角色自己评论这份相像：设计约束就变成了笑点而不是缺陷。

**负向约束要从本镜自己的正向决定推出来，不要用一张固定清单。**一块「no text, no modern objects, no extra fingers」带进每个镜头，会让每一镜*真正的*设计决定没有防护，反复出现的失败就住在这个缝隙里。每一个值得做的正向决定都值得镜像一份：

| 正向决定 | 它需要的负向镜像 |
|---|---|
| 盒子是素木，金色只作发丝般的勾边 | no gilded box, no gold relief panels, no metal casing |
| 卷轴空白，标题在合成时叠上 | no calligraphy, no characters, no writing on the scroll |
| 本镜没有任何活物 | no people, no presenter, no figure addressing camera |
| 说书人说话，其他人都安静 | no other character's mouth open, no second speaker |

拼法是：四个固定块（AI 感／非电影感构图／光／色）+ 一个类型块 + **一个按本镜自身约束逐镜生成的连续性块** + 有人时的解剖块 + 收尾的 `no watermark, no text, no logo`。然后去重。上面四种失败各赔了一个重出的镜头或一条重做的片段，每一种都有一条没有镜像的正向条款。

**每句台词说完留大约一秒的静默。**在提示词里要它。否则生成的语音会一直跑到最后一帧，剪辑时无处下刀，把拼接后母带的峰值推向满幅，每个接缝都听得出来。

做口型测试时，每条提示词都要求脸部完整可见、嘴部无遮挡。一个构图优美但说话人是侧脸的镜头，什么都测不出来。

出图之前先把提示词交给用户审。

### 2.4. 难镜头先在三维里排一遍（他人做法，本地未验证）

一段用 MCP 连接器让 Agent 操作 Blender 的教程（2026-09-16 看）说明了一件事：几何关系难的镜头，先在三维里把**镜头路线**排出来再生成。例如 10 厘米高的主角在地毯上逃跑、被吸进吸尘器管道、镜头最后退出来露出产品，一镜到底。路线在灰模阶段看清楚、改顺——哪个转弯别扭、产品何时出现——再把预演渲染连同角色图、产品图交给视频模型。三点值得抄：

- **小人故意做得粗**：方块加球体，群舞直接用方块。这个阶段只回答站位、朝向、镜头何时经过谁。但本镜真正需要的动作关系要交代：握手、翻越障碍、手要碰到某个东西。
- **用颜色标朝向**：绿色面是正面、红色面是背面，镜头绕过去就知道拍到的是脸还是后背。
- **写清每份参考管什么、哪些不要继承**：预演视频管镜头路线、站位和时间节奏；角色图管长相服装；场景图管环境风格；提示词管动作、表演和声音。然后明写：灰色小人、站位颜色、角色图的影棚背景和中性站姿都**不要**出现在成片里。这和上面「参考图太强会被照抄」是同一个坑，这里用分工来解决，而不是丢掉参考。

本地未验证：这套要求视频模型能接受**视频**参考（预演片），只吃首帧的路线不适用，用前先确认模型接什么。

### 2.5. 先出人物卡，再出镜头关键帧

角色的身份参考应该是一张**卡，不是影片里的一帧**。在中性灰背景、柔和均匀的光线下生成，不要硬阴影和轮廓光，提示词里别出现「studio」之类的词；正面全身、背面全身，再加一张大的脸部特写。之后脸只从那张特写引用，这样远景里一个又小又糊的头永远不会成为真相源。

原因是机械的：**参考图里的其他一切都会被吸进角色。**背景、色调和阴影方向会跟着进入之后每一个引用它的镜头。把一张已通过的*场景*帧当身份参考交给模型——显而易见的做法，毕竟它已经通过了——正是素袍回来带了刺绣、提示词没动服装却漂了的原因。凡不是这个人的东西，都放在卡外面。

每个反复出现的**状态**各出一张卡，不要在镜头提示词里描述变化：笑与不笑、每套服装、每个年龄、受伤前后。声音也照此办理——每个角色一个声音模块，原样引用，绝不逐镜重新即兴。

**一套卡可以自相矛盾，原因是一个没人做的设计决定。**第一轮卡（2026-09-10）正面视图长出了花瓣形胸衣、前面缀一颗宝石，背面视图却光着；一个发饰只出现在一个视图上；同一个角色三张卡三种发型。设定里没说上身穿什么、头发怎么梳，没写明的属性在每张图里都会重新发明一次。先写正向决定——衣物、领口、发型、「每个视图都一致」——再在负向里镜像它；没有正向决定，负向就无从镜像。别去压力测试一套自己都不一致的卡：对一张不一致的卡做十六次探测，量出来的只是那份不一致。

**影片依赖某个角色之前，先给它做压力测试。**一张在某个姿势里看着对的卡还不算资产。用这张卡把角色生成到影片真正需要的一组状态里——说话、大笑、低头、四分之三侧转、双手举到脸旁、远景里显得更小——然后把每张脸裁下来拼成一张拼版，看拼版下判断，不要一张一张看。

及格线必须是**看之前**就定好的数字，否则就变成「看着还行」：事先说明探测数量和容差——比如八次探测里至多一次可以看起来像另一个人，固定身份标记（发际线、饰物、眼形）的任何漂移都算失败，哪怕脸保住了。没过线就重做卡，不是重做镜头。这仍是结构化的视觉判断，不是度量指标，汇报时也要照实说；它的价值在于阈值和证据在第一个真镜头之前就存在，而不是在成片之后。

说清楚探测要花多少。在按额度计费的出图账号上，这是每个角色在任何关键帧出现之前的八到十次生成，这笔预算由用户定，不由你定。
### 3. 出图与审图（第三道闸）

生成关键帧后，**先自己看一遍**再拿给用户。检查：跨镜头的角色一致性、脸和嘴是否可见、景别是否有变化（几个镜头构图雷同会让成片显得呆板）、以及任何风格化的生物或面孔是否掉进了恐怖谷。

把不好的地方讲得和好的地方一样清楚，然后让用户决定。返工时把被替换的版本归档，而不是直接覆盖；同时收紧角色设定，让这次的修正在所有重出的镜头上都成立。

**前后一致的偏差不是漂移，两者处理方式不同。** 一个角色在*每一个*镜头里都比角色设定里写的老十岁，并没有破坏连续性——成片是能接得上的。把它当作偏离设计如实报出来，说清重出要花多少，让用户决定；不要悄悄重出，也不要把它记成连续性失败。漂移是指同一个角色在镜头*之间*长得不一样，那才是必须修的。

有些观感模型就是不肯放弃。一个本该看起来十六岁的角色，三次都回成了二十几岁（2026-09-10）——一次把年龄写成形容词，一次写成身体比例加参照物——第二次和第三次没有任何区别。这个题材对「好看的年轻成人」的拉力，两种写法都压不过。两次尝试毫无进展之后就别再花钱了：要么接受这个观感，要么把这个节拍从脸上挪开——如果笑点是「他还只是个男孩」，用一句台词说出来就行。

关键帧应该表现 t=0 的状态，即动作发生**之前**——运动是视频要补上的东西。如果图片已经画出了结果状态，那个节拍就没了。

**连续性参考太强会被照抄，而不是被参照。** 为了让新镜头「留在同一个房间里」而挂上一张已通过的关键帧，可能会把那张关键帧几乎原样还回来。一个本该从门口远景起、角色小小地站在房间另一头的主观视角开场，回来的（2026-09-15）却是为连续性而挂上的那张已通过的中景背影——角色占了画面高度的大部分，镜头没有地方可走。当新镜头必须在**景别或距离**上与已通过的画面不同时，把那张画面从参考里去掉，只保留角色卡和场景卡，并在提示词里用画面高度的比例写明景别（「她大约占画面高度的一半」）。

**参考片剧照会把它的布景和面孔一起带进来。** 一张「只作构图和光线参考」而挂上的剧照，连同整套布景——展架、横幅、灯笼——都被复制了出来；一张贴脸的侧面剧照回来的是参考片演员的脸，而不是已通过角色的脸。当目标就是复原参考片时，这是最快的路；当目标不是时，这就是泄漏。无论哪种情况，都要拿结果的脸对照角色卡，并且要预料到由剧照搭出来的房间会与此前由场景卡搭出来的镜头断掉连续性：先按新观感重建场景卡再继续，别让两个房间轮流出现。

**用根结构签名审重复，别靠眼睛。** 「景别要有变化」是谁也检查不了的建议。改为给每个共用同一套反复出现设定的镜头贴上同样十项属性的标签，再比对标签：

```
visual proposition / dominant shape / spatial structure / subject scale and count /
light structure / colour structure / stillness vs motion / what rewards looking /
central device or none / viewpoint and camera distance
```

两个*地点*不同但签名大体一致的镜头，就是同一个镜头换了层皮，连着几个这样的镜头，不管背景怎么换看起来都是静止的。一位出镜旁白者的十个镜头这样打分，十项里有九项完全相同——逐镜头看时却通过了。签名重复的地方，改结构属性，不要改布景。

**这十项属性的分量并不相同。** 一集里的四个点名镜头（2026-09-10）纸面上有区别——画面左还是右、暖光还是柔光、抬手臂还是抬前肢——比对也判它们不同。到了屏幕上就是同一个镜头放了四遍。真正看得出来的属性是**景别、画面里有几个主体、摄影机站在哪一侧**；改其中一项（把单人特写换成过肩双人镜头）立刻就打破了重复。要用强属性来区分镜头；靠弱属性凑出来的「无重复」只在纸面上成立。

对一组必须归属在一起的镜头，出图之前先把三份清单明写出来：**什么保持不变、什么允许变、以及每个镜头自己的视觉主张。** 角色设定提供第一份；没有第三份，每个镜头都会继承模型默认的构图。

### 4. 托管（第四道闸）

首帧模式需要把每张图放在视频 API 取得到的地方。出图之前先选路线，因为路线决定了这道闸到底存不存在：

| 路线 | 需要托管？ | 状态 |
|---|---|---|
| 图像模型返回托管 URL（`agnes-image-2.5-flash`，`response_format: url`），同一个 URL 交给 `agnes-video-2.5-flash` | 否 | **已验证** 2026-09-11 |
| 视频 API 接受以 Base64 data URL 内联的图片（MiniMax 的首帧参数，按其文档） | 否 | 仅有厂商文档 |
| 传输不计量的静态托管——Cloudflare Pages 预览部署 | 是 | **已验证** 2026-09-25/26：视频服务商从 `*.pages.dev` 取图（回片第一帧与托管关键帧一致），随后跑完一整轮 8 条 |
| 自带过期的临时静态托管——Firebase Hosting 预览频道，或带签名 URL 的对象存储桶 | 是 | Firebase 多轮**已验证**，但其免费传输额度用光了（见下）；对象存储未测试 |
| 隧道后面的本地 HTTP 服务器（Cloudflare quick tunnel） | 是 | **已验证** 2026-09-11 |

2026-09-11 验证的两条路线教会的事：

- **服务商的托管 URL 跳过了这道闸，但跳不过证据。** Agnes 图片 URL 不带签名也不带过期参数，视频完成后仍然能取到，但它的保留期没有文档说明。返回的那一刻就下载一份副本并算哈希；归档的是副本，不是 URL。
- **单靠本地服务器是够不着的。** `localhost` 和局域网地址对服务商的服务器毫无意义，而多数家庭网络没有公网地址。隧道提供一个。把服务器绑到 `127.0.0.1`，只提供一个仅含关键帧的目录，任务完成后两者都拆掉。
- **从外部验证可达性，不要走本地解析器。** 第一次尝试在自己的预检就失败了，因为本机解析器解析不了刚建好的隧道主机名；看起来像隧道死了。用公共 DNS 服务器解析，通过那个地址取图，比对哈希，然后才提交。
- **服务商只取了一次图。** 服务器日志显示提交后约十五秒有唯一一次请求。尽管如此，URL 仍要保持到任务完成——服务商文档就是这么要求的。
- **检查路线在用户所在的地方能不能用。** 在一个网络能用的路线在另一个网络可能失败：某些 DNS 服务器、UDP、或服务商的存储域名可能不可达。在整轮依赖它之前，先从用户的网络测一遍。

临时静态托管优先选静态传输不计量的；Firebase 预览频道也能用，还自带过期，但先看下面的额度说明。 每次上传都用一个**新建的、专用的**频道或预览部署；任务记录已经引用的地址绝不重新发布覆盖，覆盖等于毁掉证据链。后续轮次可以原样**沿用**上一轮的部署不重发，每轮开始时把哈希重新核对一次即可（见「多轮沿用同一个临时网址」）。

托管之前，先把上传目录搭好并扫描：API 密钥字符串、通用凭证特征、以及图片元数据里的路径、提示词原文和身份信息。某些生成器的图片会嵌入 C2PA 来源清单和不可见水印——内部测试无妨，但要说明，且在把这类图用于公开交付物之前想清楚。

**免费托管按流量计，排队会把流量放大好多倍（2026-09-25）。** 一个月里几轮排长队的出片，用光了一个 Firebase 项目免费档每月 10 GB 的托管流量。这个额度按项目计，项目里所有站点、预览频道、自定义域名共用；宽限期一过，项目里所有站点都会停用，连不相干的正式页面也一起停。大头出在发送脚本自己：每次提交前都把托管的关键帧重新下载一遍核对哈希，而繁忙的队列拒绝了大约一千次，每次约 3 MB。每个托管文件只核对一次并记下来，重试时跳过下载。关键帧托管要么单独放一个项目，要么放在静态流量不计量的地方（Cloudflare Pages 文档写明各档静态请求都免费且不限；单文件 25 MiB、每站 20,000 个文件；没有自带过期，旧的预览部署要自己删）。

**用 Cloudflare Pages 托管关键帧（2026-09-25/26）。** 这次切换教会的事：

- **每次部署都有一个不会变的网址**（`https://<hash>.<project>.pages.dev`），另有一个分支别名，下次部署到同一分支就会指向新内容。提交和记录都用不会变的那个，它相当于一个新频道。
- **没有自带过期。** 申请批准时写清清理办法，而不是过期时间；这一轮结束后删掉旧的预览部署。`wrangler pages deployment list --json` 只给相对时间（「9 minutes ago」），清理脚本要自己解析。
- **Cloudflare 对 Python 默认的 `Python-urllib/x` User-Agent 回 403**，curl、`python-requests`、Go、okhttp、浏览器标识都是 200。自己核对下载时一律带别的 User-Agent；视频服务商取图没被拦（回片第一帧与托管关键帧一致）。
- **新建项目后几秒内的第一次请求 TLS 握手失败**，一分钟后就正常了。先重试一次再下结论。
- **当前 `wrangler`（4.140）把 `pages project create` 转给 Workers**，那条路拒绝建空项目；加 `--force` 留在 Pages。新项目官方主推 Workers 静态资源，本地没试过。
- 上传目录里放一个 `_headers`，给所有文件设 `Cache-Control: no-store` 和 `X-Robots-Tag: noindex`；放在临时副本里，调用方的文件夹始终只有图片。

事先讲明：目标、过期时间或清理办法、精确的文件清单（含字节数与哈希）、执行命令、一份**不会**被触碰的东西的清单（生产站点、其他频道、数据库规则、认证设置、计费套餐）、如何验证、以及如何回滚。

托管之后，匿名 GET 每一个 URL，把 SHA-256 与本地比对，再重新列出托管目标，证明生产环境和之前的频道都没被动过。结果存成 JSON。

### 5. 提交与合成

每个镜头提交一个任务，或每个编排式多镜头段落提交一个任务（见「编排式多镜头段落」）。在 POST **之前**就把任务记录落盘，这样中断的提交可以恢复，且绝不会静默重发。用返回的 video id 轮询。

**重试任何东西之前，先按落盘状态给每个失败分类。** 一批回来一半失败的任务里，至少混着三种在汇总行里看起来一模一样、处理方式却相反的情况。一次 18 镜的运行（2026-09-09）里 9 个失败，拆开是：

| 状态 | 任务记录里的证据 | 正确做法 |
|---|---|---|
| 任务还活着，只是*轮询*失败 | 有 `video_id`；最后一个错误是限流 | **重新轮询。绝不重提。** 任务还在跑 |
| 提交被明确拒绝 | 没有 `video_id`；错误体是队列满／拒绝码 | 归档记录并重提——服务端什么都没建 |
| 任务在服务端真的失败了 | 有 `video_id`；状态 `failed` | 归档并提交一个**新**任务 |

这 9 个里有 3 个只是被限流，任务本身跑到了完成；一刀切地重提会白烧 3 个计费任务，还留下 3 个孤儿任务在跑。把这个区分做成**守卫，而不是习惯**：凡是记录里有 `video_id` 且没有失败的，拒绝归档或重提。

一个把每次抛错的提交都标成 `submission_uncertain` 的客户端是恰当地保守，但错误体通常能说明是哪一种。明确的拒绝（「queue is full」）意味着什么都没建，重试不是重复 POST；超时或连接中断才是真的不确定，必须通过查询来核对，绝不靠重提。

**退避应该放在调度器里。** 用紧循环轮询每个待完成的镜头会招来限流，而被限流的轮询看起来和失败的任务一模一样。镜头逐个轮询、中间留间隔，遇到服务商的限流码按指数退避，不要把它们当成判决。

**重试必须先清掉被拒的记录，否则永远发不出去。** 客户端在 POST *之前*落盘任务记录，这是对的：中断的提交可以恢复，绝不会静默重发。但这意味着一次被「queue full」拒绝的提交会留下一条记录，调度器的退避重试随即被客户端自己的防重复 POST 守卫挡住。一轮之后加上的 503 退避因此从来没有真正起过一次作用；那轮看似被它救回来的，其实是靠一条手工归档的记录救的。每次重试之前先归档过期的记录——仅当它没有 `video_id` **且**错误是明确拒绝时；超时或连接中断仍属不确定，必须通过查询核对。记录清不掉就停下来，别空转。一条从未干净跑通过的自动路径，在没人手工兜底之前看起来和能用的一模一样。

合成时先做归一化（统一缩放、帧率、像素格式；静音轨补齐；响度归一化），再做拼接。从成片里抽帧，然后自己看。

### 6. 记录

写下产出了什么，并把**未验证**的事项——语种判定、账单、任何异常——单独列一节，而不是埋在正文里。等用户后来给出判定，再回写，并写明是谁判的。
## 从本地控制台驱动

这套流程原本是在对话里一步步走的，后来包了一层本机网页画布，好让不写代码的人也能跑。包装过程中得到的经验，按踩到的先后：

- **文件才是产物，页面只是视图。** 磁盘上已有的每一份状态——每个资产*版本*一个任务 yaml、每轮生成一个文件夹、每版剪辑一个剪辑单——一直是真相源。页面只读写这些文件、自己什么都不持有，于是同一个项目在终端、agent、页面三边都同样能接着干，做到一半的会话也不会有状态困在浏览器里。
- **先出计划、确认、再执行，计划要写清楚。** 每次调用服务商都要经过一份人来确认的书面计划：几次调用、多少秒、多少钱、哪些东西会公开、哪些因为已存在会跳过。确认的时候重新校验一遍，底层文件自出计划后变了就拒绝——计划是对世界的断言，而世界会动。
- **任务记录先落盘再调用，不是调用完再落。** 服务重启时把当时「进行中」的标为*中断*而不是丢掉；继续要重新出计划、重新确认。已经拿到服务商任务编号的记录只查询、绝不重提——和第 5 道闸是同一条保护，现在由真正负责提交的那一环来执行。
- **按实际产出记账，不按计划记账。** 三次因 key 被拒的失败也被记成了花费（2026-09-16），因为账本信了计划。金额要从这次运行自己的结果里取，每个任务只记一次，失败就记 0。
- **凭证只检测、不持有。** 页面只看命令行登录了没有、环境变量设了没有、凭证文件在不在——只显示这些，外加修复用的命令。不读值、不回显、不存进仓库、不通过表单收。要明说环境变量配好后需要重启服务才看得见。
- **只提供真正接好的选项。** 项目可能用到的模型可以全列，但要标明哪些工具真能驱动、哪些只是记录选择，未接入的在出计划时就用那句话拒绝。灰掉并写明原因，好过跑到一半死掉。
- **给生成物盖归属标记。** 通用渲染器产出的剪辑单没有标记，归属靠一个「编辑于」字段推断——而编辑器每次保存都会改写这个字段，于是改过的副本被交给了错误的渲染器并崩溃（2026-09-17）。要单独写一个明确的 `renderer:`（或等价）字段，编辑后仍保留；老文件的兜底判断看结构，绝不看署名字符串。
- **相对路径属于文件最终落地的位置。** 第一份自动生成的剪辑单按默认目录算片段路径，实际却写到了别处，渲染器什么都找不到。按真实目标目录算。
- **按台词剪，并把剪辑交出去。** 初剪按每段自己的逐词时间戳台词范围生成（见「每个片段都从第一帧开始裁」），同一份剪辑单还能导出 OpenTimelineIO 与 Final Cut XML，拿到正经剪辑软件里收尾，不用重新手工对轴。
- **界面顺序照着流水线的依赖来。** 先卡、再关键帧、关键帧批准后才做视频、视频之后才剪。每一镜直接列出它引用但还没批准的卡，并让那一行就是修复按钮；每类资产给一个默认版式模板（角色多角度定妆、场景同光线两机位、道具三视图），让第一张卡是一次点击而不是一个空白提示词框。

## 一套有旁白的教学片学到的（2026-09-16/17）

三集课堂教学片——同一位反复出镜的老师、平涂水彩绘本画风、普通话旁白、内嵌字幕——全程通过画布完成：3 集、11 轮共 44 个模型任务，7 个镜头沿用已有 PPT 插图。它给本文添的内容：

### 首帧的画风不会自动带进视频

每张首帧的提示词都要求平涂水彩，每张首帧也确实是平涂水彩。**视频**提示词没写画风，片段就漂了：皮肤出现明暗和高光、画上去的背景变得光亮，有两镜在一两帧之后直接切成了同一房间的**照片**版本。较轻的漂移是审片人在成片上用肉眼发现的；逐秒对比图（首帧、1 秒、4 秒、7 秒）让它一目了然。在视频提示词「一镜到底」那句后面紧接着加一句，问题就解决了：

> The whole clip stays a flat watercolour children's-book illustration exactly like the first frame — soft paper texture and painted colours; it never turns into a photograph or live-action footage.

**已验证**：三集 14 个老师镜头（2026-09-17）重生成的每个片段都保持了画风，包括之前变成照片风的那些。风格化的片子要把这句锁画风当成固定块，逐字节一致地放进每个视频提示词，和角色设定一模一样——出图提示词里的画风句不会传过去。

### 模型会读错的台词，以及何时别再重抽

每个对白片段都由人听过。有三种毛病反复出现，每种都不止一次：

| 台词特征 | 现象 | 有效的改法 |
|---|---|---|
| 叠词（「很久很久以前」） | 吞掉一个字，同一句**连续两次** | 改成不叠字（「好久以前的時代」） |
| 句末疑问词（「……甚麼？」） | 像外国口音，**连续三轮**——加强声音设定改善了句子其余部分，但救不了最后那个词 | 改问法，不让这个词落在句末（「人和狗是怎樣互相幫忙的？」） |
| 多音字（「種子」） | 声调读错 | 换成没有歧义的词（「穀粒」） |

同一个毛病出现**第二次**，就改台词，不要再重抽。新台词在第 2 道闸之前先用程序筛：无叠字、句末无疑问词、能换就不用多音字。本机语音识别可以当绊线（两次都把吞字那句转成「很久久久」），但绝不能当裁判——它也会把正确的台词转成近音字（「穀粒」→「古力」、「馴服」→「巡浮」）。

### 声音样本，或声音描述

这个视频接口没有声音编号，首帧模式也不收已通过的录音——只有 reference 模式收声音样本（见下）。在首帧模式里能固定不变的是声音设定那一段，所以要审计它：把每一轮每次提交的提示词里的 `VOICE:` 段落抽出来比对哈希。这次审计查出第一集有三镜在其余镜头都换过之后仍在用修正前的版本。并且要明说：**每次重新生成都是新录音**——之前听审通过的台词必须重听，哪怕唯一的改动只是一句画风。

加强版声音设定确实有用：把说话人写成母语普通话使用者，"like a primary-school Chinese teacher on national television … no foreign or non-native accent"，去掉了听审人标出的大部分口音。

**已对照供应商模型页查证（2026-09-21）**：`agnes-video-2.5-flash` **没有**任何 voice、speaker、音色、`voice_id` 或 TTS 参数。唯一的音频输入是 `audios`——最多三段参考音频，提示词里用 `<Audio N>` 引用——而它**只在 `reference` 模式存在，该模式不收 `first_frame`**。所以要钉住声音，代价是首帧不再是第一帧；整个决策就在这一个取舍上。同家模型上的网页端提供「上传 5 秒样本」和「用描述生成音色」两种可复用的音色 ID。供应商唯一记录在案的示例是拿 `audios` 参考节奏和氛围（「以 `<Audio 1>` 的节奏和环境氛围作为参考」），从没拿它当声音。

**实测一次（2026-09-21，flash，一组 A/B）**：从成片里截男主本人 5 秒人声（归一到 -18 LUFS，单独开 24 小时频道托管）作 `<Audio 1>`，配一句样本里从没说过的新台词，提示词写明只把它当作 "only as the sound of his voice: do not play it, and do not say any of its words"。对照组 B 只少这一段和 `audios`，其余全同。flash 接受了；语音识别两片都听到新台词、没有样本原词（绊线通过）；**听的人判定 A 比 B 更像样本**。粗测基频却*没*显出来（A 120 Hz、B 119 Hz、样本 131 Hz）——耳朵听出了数字没测出的东西，所以别拿基频当判定。同一组里看到的代价：reference 模式重构了画面（近景变中景），脸也漂得偏年轻、偏精修。一组只算线索不算定论：整部片要依赖它之前，先换第二个角色、第二句台词再测一次，并逐镜权衡丢掉首帧的代价——声音样本适合构图可以漂的说话镜头，不适合走位钉死构图的镜头。

**怎么附声音样本：**

1. 截 4–6 秒角色单独说话的声音——取自已通过的片段，或用户提供的录音——不能有音乐或第二个人声。归一化（`loudnorm=I=-18`）后导出 mp3。
2. 像关键帧一样托管：单独的预览部署或频道、**单独确认**（这是一次部署），然后核对 HTTP 200、`audio/mpeg` 和与本地文件一致的 SHA-256。
3. 用 reference 模式提交——不放 `first_frame`；已通过的关键帧放进 `images` 当画风／长相参考：

   ```json
   {"model": "agnes-video-2.5-flash", "mode": "reference", "seconds": "5", "size": "720P",
    "aspect_ratio": "16:9", "n": 1,
    "images": ["https://<channel>/S15.png"],
    "audios": ["https://<voice-channel>/voice_C1.mp3"],
    "prompt": "... VOICE: he speaks in exactly the voice of the man speaking in <Audio 1> — the same timbre, pitch, speaking rate and accent. Use <Audio 1> only as the sound of his voice: do not play it, and do not say any of its words. ..."}
   ```

   文字版声音设定照样留在提示词里；样本是加上去的，不是替代。
4. 核查：语音识别必须听到新台词、且没有样本原词（绊线）；把输出画面与关键帧对照，看景别和脸有没有漂；然后**由用户来听**——agent 判不了声音，基频数字也不算结论。记下判定和判定人。
5. 每个任务最多三段 `audios`，所以双人镜头可以挂 `<Audio 1>` 和 `<Audio 2>`——未测，依赖前先做 A/B。同一页还列了本流水线从没用过的 `seed` 参数，并确认单片段 **4–12 秒**：网页端计划 15 秒的片段回来只有 12.26 秒。

**声音卡只放声音。** 卡尾写着「本镜只有他一个人说话，其他人全程闭口」，是把逐镜的编排塞进了逐角色的卡；原样抄进一个两人对话的片段就自相矛盾。音色、音高、语速、口音、语气放卡里；*这一镜谁说话*放镜头里。

### 卡片关与卡片形态

- **不能跳过 2.5 关。** 这次卡片出完直接拿去当镜头关键帧的参考，没给用户看；被用户指出。顺序是：卡片先批准，再压力测试，然后才能用。
- **看得见的属性先写清楚再出卡。** 设定没写裤子鞋子，模型自己发明；出卡后才补，卡和设定就对不上，只好出 v2。
- **模型从来不画的标记当不了认人标记。** 梨涡写在设定里，卡片和 8 张测试图都没画出来。记下来，保留与否由用户定，不计入及格线。
- **不同视图会串色。** 背面卡开襟衫比正面偏粉；附上正面卡当参考、在提示词里写死颜色后重出即可。
- **画布里一个角色一版只对应一张卡**，不是一个视图一张：把正面、背面、脸部拼成一张三视图，各视图任务另放，批准走画布自己的审批调用。

### 沿用已通过的素材

已有 PPT 插图的画面镜头直接拿那些页面当首帧——不出新图，片子也和课堂材料一致。7 个这样的镜头全部成功。为别的用途画的图可能带着影片镜头不该有的东西：第八张候选图里有两个卡通小孩和写字板上的假文字，于是重画成只有器物、刻痕抽象的静物。

### 跨项目的调用频率限制

两个画布项目同时提交，第二个项目的第一个任务收到 **HTTP 429 `rate_limit_exceeded`**（「免费用户」）。返回体是明确拒绝、记录里没有任务编号，所以什么都没建；画布却把它标成 `submission_uncertain` 并停下整批。这样的记录存档，然后按同一个已授权数量重新提交。更好的是一次只提交一个项目的批次。报错文字邀请升级付费方案——那是账号主人的决定，绝不是 agent 的。

### 多轮沿用同一个临时网址

后续轮次沿用第一轮的预览频道、不重新发布：每一轮开始时匿名重新下载每张图核对哈希，把结果写进新轮次，并检查频道剩余寿命。每轮核对一次并记下来——不是每次提交尝试都核一遍（见第 4 道闸的流量额度说明）。必须在频道过期前跑完的轮次，申请批准时就说明。

### 后期踩到的坑

- **倍速版**：画面用 `setpts=(PTS-STARTPTS)/k`、声音用 `atempo=k`（音高不变），字幕时间按同样倍数缩；片头字卡、静物推镜、结尾停留保持原速。每个倍速单独一个剪辑单版本和成片名。**核查渲染器真的用了 `atempo`**：有一个给画面加了 `setpts`，却用 `atrim` 把声音截到新时长，1.25 倍版悄悄丢掉了每句台词的最后五分之一（2026-09-21）。另外用 ffmpeg 从成片切正文去变速时，`-t` 要放在 `-i` **前面**——放在 `-i` 后面限制的是*输出*时长，变速后的正文根本够不着，于是整条成片连片头字卡一起被变速，字卡还出现了两次。
- **`alimiter` 默认会归一化。** 它的 `level` 选项默认开启，会把整段混音拉到上限；凡是只想把限幅器当天花板用的地方都加 `level=false`，然后每个成片重新测。
- **字幕在标点处换行。** 按字数折行把「市集」拆成两行；一行里前三分之一之后有逗号或顿号时，在该行最后一个逗号或顿号后断。
- **繁体检查会矫枉过正。** OpenCC `s2hk` 把「群」改成「羣」；按项目已定的用字留一份短白名单，其余一律报错。
- **品牌角标写在剪辑单里，不写进提示词。** 生成器被要求不出 logo，所以角标在合成阶段叠：剪辑单加 `watermark`（叠加用的 PNG、是否跳过片头字卡），文件不在就大声报错。叠*静态* PNG 时输入要加 `-loop 1`——不加则叠加层只有一帧，第一帧之后就没了。用 `enable='gte(t,<title_end>)'` 让它避开片头字卡，带角标的成片另起成片名渲染，不覆盖干净版。
- **绝不覆盖带版本号的文件。** 一份新剪辑单存成了已存在的版本号，毁掉了一个 1.5 倍速版（不在 git 里）。它照 1.25 倍速的同胞版重建、并对着已渲染的时间线证明一致，但规则更简单：先确认文件名空着，被占用就报错。

## 把整条流水线搬进控制台之后踩的坑（2026-09-18/19）

之前是 agent 在仓库里写提示词和任务文件、画布负责显示。把「写」也搬进控制台之后，冒出的是另一类问题：不再是「模型干得好不好」，而是「操作的人到底知不知道现在在发生什么」。

### 提示词从项目里拼出来，不是让人从头写

一镜的出图提示词不是非得从空框开始的创作：项目里早就有这一镜的调度、出场每个人的设定卡、道具、台词和整体画风。把它们拼好，让用户在上面改。拼装是确定性的——不调模型、不花钱、不用等——所以任务对话框可以永远打开就是填好的。验证过的形状：

    image  【frame】调度 + 运镜
           【<名字> · copy verbatim】人物设定卡，**一个字不改**
           【<场景> · copy verbatim】场景设定卡
           【<道具>】画面里的每件道具
           【style】项目那一句画风
           【never】不要文字、字母、数字、标签、字幕、水印、签名、logo；不要多指
           【not in this shot】只有这一个人，或者一个人都不要

    video  一镜到底那句
           锁画风那句，**这里要再写一遍**——关键帧的画风不会自己带进视频
           场景散文，然后发生什么，然后机位
           VOICE: 声音设定卡，**一个字不改**，以及谁是唯一说话的人
           台词，标注 "say exactly this, do not change or add words"
           不要字幕、不要屏幕文字、不要水印、不要 logo

两个字段**原样照抄、绝不转述**：人物设定卡和声音设定卡。转述任何一个，脸或声音就会在镜头之间漂，而且要等片段并排放在一起才看得出来。

### 画外音镜头要反过来写

拼装器第一版看到「这一镜有台词、有人物」就热心地加了一句 *"…is about to speak; face and mouth completely unobstructed."* 它加在哪一镜上呢？那一镜写的是：*"voiceover narration, the old man has his back to camera, no lip sync."* 正好相反，而且会直接踩中这条流水线最常见的毛病：只要音轨里有声音，视频模型就会找一张脸去对口型。

所以拼之前先判断镜头类型。调度里写了画外音、旁白、背对镜头、不对口型，就把所有指令反过来：出图提示词写嘴不动，视频提示词写画面里的人不说话、台词是旁白盖在他们上面的。按关键词匹配调度文字就够用，而且值得做，因为这个毛病是静默的——片子看着没问题，直到有人发现嘴在动、说的却不是这个角色的词。

**这套反写只在旁白不由视频模型生成时成立。** 台词走 TTS 音轨、在合成时铺上去；视频提示词是一条*无台词*提示词（中文、结构化声音段、人声栏写 无台词），里面绝不出现那句台词。让视频模型自己在一张可见的脸上出画外音旁白，探针能过、正式生产必翻车——模型会给音频找一张嘴（见「实测结论」）。旁白非得由视频模型出，就给它一个出镜的说话人。唯一的例外是画面里没有脸的画外音镜，它可以在 `reference` 模式下挂一段钉住的声音样本来带台词（见「实测结论」）；拼装器遇到任何画面里有脸的画外音镜，必须拒绝把台词写进视频提示词。

### 台词卫生可以查，不用靠运气

本文件前面那张「模型会读错的台词」表，是一次次重抽攒出来的。其中大部分是机械规律，所以在有人为它花一次视频调用之前就该查一遍草稿：两种形状的叠字（`AA` 如「看看」、`ABAB` 如「很久很久」）、句末疑问词、长到会念飘的句子、薄到撑不住一张脸的人物设定。这些报成提示而不是闸门——人可能有他的道理——但要在草稿出现的那一刻就报，而不是等片段回来之后。

### 模型出的草稿绝不能直接写进项目

文本模型一次给出整份镜头表时，最省事的做法是直接写进项目文件。别这么干。写成一份可丢弃的草稿，逐镜列出来带勾选框，批准之后**走跟手改完全相同的那条校验保存路径**——同样的编号规则、同样的「这个场景不存在」拒绝、同样的「已经出过关键帧的镜头不能删」。这样草稿永远可以丢，「应用」永远不是特殊通道，一次糟糕的生成只值一次把它丢掉的点击。

### 控制台必须把自己在干什么说出来

这一周三次「它卡住了」的报告，全都是控制台没有把正在正常进行的工作讲出来：

- **长任务没有进度行，看起来就是按钮坏了。** 一次两分钟的分镜调用什么都没显示，因为页面从一个根本不带任务的接口读任务状态。要显示状态、已经跑了多久、完成之后会怎样；完成时自己刷新页面。
- **「排队中」什么都没解释。** 要说在等什么：「同一时间只跑一个出图任务；在等「…」跑完」。否则一个正确的队列看起来就是卡死，用户就开始乱点。
- **批量操作必须把还不存在的工作算进去。** 「批量出关键帧」只看已经建好的任务，于是在一个 12 镜、只手建过 1 个任务的项目上报了*全都生成过了*——而 11 镜连任务都没有。跨流水线阶段的批量要覆盖还没开始的、开始了没完成的、已经完成的三种，动手之前先说出每类各有多少。

### 一些让控制台诚实的小事

- **多选列表要有全选和实时计数。** 一列没有计数、没有全选的复选框就是手工活；主按钮应该写清它会拿这份选择做什么（「创建任务（带 3 张参考图）」），别让人凭空一跳。
- **确认审批生效了。** 点「通过」之后整个对话框重绘、页面一跳，读起来像什么都没发生。顶上一行状态、点击后短短一句确认，就够了。
- **用字要跟项目一致。** 标签写死成繁体的拼装器，在简体项目上拼出半简半繁的提示词。按项目自己的文字判断。
- **密钥绝不放在代码所在的地方。** 密钥存在仓库外、权限 600，shell 自己的环境变量优先于存的那份，只回显末四位，日志一个字不记。另外：判断「配好没有」的那段代码，必须读服务商真正读的同一个地方，否则面板说就绪、每次调用都失败。

### 检测别人家工具拥有的路径

一次跨模块的全文改名（`credentials` → `keystore`）把*数据字符串*也跟着代码一起改了，于是检测 Claude CLI 登录状态的代码去找 `~/.claude/.keystore.json`。用户反复登录，面板一直说「还没登录」。任何指向别的工具所拥有的文件的字符串都是数据、不是代码：把这些路径集中放一张表，并用测试断言确切的值把它们钉住。

相关：用「命令存在、凭证文件存在」来检测一个 OAuth CLI，看不出 session 已经**过期**。一个被超时踢下线的 CLI 能通过检查、在调用时才失败。界面上要讲清楚，别暗示一个绿勾意味着下一次调用一定成。
## 编排式多镜头段落（2026-09-21）

一部 28 镜的喜剧按本文的做法一镜一个片段生成。同家模型上的网页端则把一整段戏——三四个镜头——塞进一个片段，每个镜头写明秒数、景别、机位和落幅。本流水线上的一次对照证明这能力在这里同样成立，而且它的写法和本文的走位正好互补对方的短板。

### 要它做时，模型做到了什么

同一段夜街戏、同一张首帧、同样的台词，只改一个变量：

| | 一镜一片段（S14/S15/S16） | 一个编排片段，3+6+3 秒 |
|---|---|---|
| 生成次数 | 3 | 1 |
| 切点 | 按设计没有 | 3.08 秒和 9.92 秒，恰好两刀 |
| 台词 | 每片段一句 | 三句都在，各在自己那一镜 |
| 正反打 | 两个特写背后是同一片天际线——作弊 | 第 1 镜背后是浦东，反打背后是对岸外滩——地理正确 |
| 跨切点的声音 | 靠剪辑拼 | 连续；她的台词比切到她早 0.5 秒开口，压在他的特写上，他全程闭嘴——干净的反应镜头 |

账同样重要：按场次打包，28 镜约 **9** 次生成；在一次提交可能排队二十分钟的免费档上，这差不多就是一整天。

### 它会错在哪，以及为什么走位能治

**人被复制。**「她的肩膀虚焦在前景」没说哪一侧，结果出了*两个*一模一样的她，一左一右夹着他。**画面方向翻转。**第 1 镜她在左他在右；越过他肩膀的反打却变成他左她右——机位越轴了。网页端自己的样片还有本文早就在防的第三种：说台词的人脸被裁出画面。

三个都是几何问题，而走位表里本来就有。所以编排段落要*从*走位写出来：

- **在所有镜头之上写一次画面方向**，取自演员站位：「她始终在画左，他始终在画右；机位绝不越到他们另一侧。」
- **越肩镜头写明哪一侧肩膀、哪一侧画框边缘**：「在她身后，只露她后脑和右肩，在画框**左**边缘；画框右边缘没有人。」
- **数人头**：「每个人在每个镜头里只出现一次」，并在负面词里镜像（没有第二个女人、不得复制人物、同一个人不得出现在画面两侧）。
- **锥形检测和口型朝向逐个子镜头跑**，不是逐片段。说话人不在画面里，出现在第 1 镜还是第 3 镜都算失败。

### 提示词的形状

    本片段由 N 个镜头剪接而成，严格按下面的时间。只在 <t1>、<t2> 秒切；没有别的切点。
    每个镜头从上一个镜头的落幅接着拍。                         ← 替换不间断长镜头句
    <风格锁>
    画面方向——所有镜头固定：……                                  ← 取自走位
    [场景] … [人物 · 逐字照抄] …
    镜头 1（0–t1 秒）——景别、高度、机位、谁在哪、发生什么。落幅：……
    镜头 2（t1–t2 秒）——……从那个落幅接。落幅：……
    声音（<角色>）：<声音卡，只含声音>                            ← 每个说话人一段
    台词——每句只在自己那一镜、由指定的人说。一字不改。
    镜头 1——<角色>：
    <台词>
    <禁字幕条款>

**落幅 → 承接**这条链才是网页端真正的手艺：每个镜头说自己停在哪，下一个说从那里开始。台词仍按本文要求单独成块、一镜一句——网页端把台词夹在舞台说明中间，语音识别在它三条样片里没发现念出舞台说明，这只是对那条规则的弱反证，不是放弃它的理由。

### 什么时候用哪种

**同一地点连续的一段戏**用编排段落——对话、揭晓、并肩走。以下情况仍一镜一片段：某一镜需要单独重做（段落只能整段重来）；每一镜都要首帧管住（只有第 1 镜有首帧，后面靠文字延续身份）；以及所有需要长镜头的镜头。段落受单片段 12 秒上限约束，超过就拆成两段。

**状态——2026-09-21 在同一段戏的两个片段上验证。**第一个没写几何，出了上面两种错。第二个只改了本节的写法——画面方向写一次、写明哪侧肩膀和画框边缘、每镜每人一次、负面词镜像——两种错都消失了：只有一个她、只在画左边缘；三个镜头始终她左他右；切点 3.12 秒和 9.42 秒，计划是 3 和 9；每句台词都在自己那一镜。收听者判两个声音可以接受。同一段戏的两个片段是强信号，不是定律——第一轮真跑时再确认一次。

### 跑一个你控制不了的队列（2026-09-20/21）

- **免费档队列有时段。** 一个下午：四小时、48 枪，全是 `video_queue_full`。次日凌晨 3 点第一枪进去；6:15 到 7:33 之间完成了二十个片段。无人值守的轮次排到清晨，用一次真实提交去探，别拿整批去砸满的队列。
- **探针和调度器是两个发送方。** 探针提交了第 1 镜后把剩下的交给画布，画布 4 秒后就发第 2 镜：HTTP 429。调度器的 61 秒间隔只算它自己发的。交接前先把间隔等够，或者让调度器连探针一起发。
- **盘上有记录不等于提交成功。** 任务记录在 POST *之前*落盘——这正是中断后能恢复的原因——所以数记录文件数的是尝试次数。要数持有 video id 的记录。
- **有截止时间的调度器要续。** 画布两小时后停掉视频任务。排队二十分钟一个，就是三个片段然后 `failed`。停了就重新确认；持有 video id 的记录只轮询不重投，所以续是安全的。
- **换 API Key 换不掉队列。** 第二个 Key 认证通过，拿到的还是 `video_queue_full`。它的计费接口显示有支付方式、额度敞开，但非 flash 提交被拒：`insufficient_user_quota, remaining $0.000000`。余额只有提交才问得出来；OpenAI 兼容的计费接口返回的是样板值。

### 一个从不报警的检测器也不是证据

镜头切换扫描在 28 个片段里找到零处内切。信它之前，先把两个片段首尾拼起来跑——接缝处命中一次——再跑单个片段——零次。一个同样报零的字幕筛查，拿一帧手动画上字幕去试，**它漏掉了**：夜景素材下三分之一的亮像素比一行字幕还多。于是丢掉它、改由肉眼判。一个安静的检测器需要阳性对照，正如一个吵闹的检测器需要阴性对照。

## 无主讲人的旁白片，以及用 TTS 出旁白（2026-09-21/23）

一支 12 镜的月饼历史短片，先按「真人出镜主讲」做完，又整个改成**完全无人出镜**：水彩插图生成无台词画面，旁白另外用语音合成出来，在本地铺上去。这套形态现在是这类讲解片的默认做法，它一次解决了四个毛病。

### 为什么放弃出镜主讲人

主讲版每一道闸都过了——身份卡、压力测试、真人脸 probe——片主看完成片仍判「不连贯」。四个原因，都不是换提示词能修的：

- **每条片一个声音。** 视频模型没有 voice id，每条现场选角；六个说话镜就是六个人，而且每条提示词里都带了完整的声音圣经——圣经能收窄漂移（更早一部片子九条都稳住了），但钉不死。
- **越难的镜头脸越漂。** 普通镜头很稳；一条片同时要求环绕、换景、换装，回来换了个人；reference 模式也会把脸改掉。
- **一集之内换造型**，观众要重新认一次人，再加上五个地点来回跳。
- **主讲人打断故事。** 传说和证据本来就有画面，人每隔几秒出来说一句，等于按暂停。

讲解片默认不设出镜主讲人。身份资产留着，不花钱，决定也可能改回来。

### 旁白单独成轨

画面与声音分开生成，在本地合成：

- **画面**：每条都按*无台词镜*生成（中文提示词、结构化声音段、表情固定）。画面里没有人需要对口型，偷嘴这个故障的概率大大降低——但最响的几个时刻仍要检查嘴：画面里有一张脸、底下压着音频，正是模型钻空子的条件。
- **声音**：TTS 按句返回音频，音色 id 指定，全片一致。台词时长在定分镜前就知道，镜头秒数按音频排，不用迁就模型出片长度。
- **改词成本**从重出一条视频降到几百字的合成费；字幕直接按脚本烧，语音识别只当绊线。

实测一家（MiniMax `t2a_v2`，2026-09-23）：系统音色 303 个（普通话 45、粤语 6，其余分散在十几种语言），六个模型，八种情绪，语速／音量／音调可调，输出 mp3 或 wav。两个要记下来的限制：**48 kHz 被拒**（32 kHz 可用）；**该账号音色克隆、「按描述造声」和配乐接口全都用不了**（被禁／套餐不支持／不再对新用户开放——见下文 31 镜试点的「配音」），只有系统音色能用。这是一次带日期的观察，不是保证。

### 两条各赔一次重渲染的音频剪辑规矩

- **语音识别的词时间是绊线，不是剪辑点。** 按它报的末词结束时间裁旁白，把「…今天这样的」切没了——它报早了。头部按静音检测裁（见下一条），尾部一直保留到文件结束。这条规矩针对的是 TTS 旁白文件，它整段保留。模型生成的对白片段不一样：那里词时间是用来*定位*台词在片段里的位置的（见「每个片段都从第一帧开始裁」），而切点仍落在词时间之外一拍，绝不落在词时间上。
- **旁白尾巴绝不做静音裁剪。** 句尾轻声字电平低于 -45 dB，会被当静音吃掉。反过来同理：按识别的首词时间裁头，把句首的「所以」切没了；裁头要用静音检测。
- **最后一个字要避开下一处转场。** 交叉淡出会把末字压掉，即使音频完整；每段留出余量，让人声至少在接缝前约 0.9 秒结束。

### 模型可能只填满画幅的一部分

好几条片回来是 1280×720，但画面只占 1178×666 或 1172×660，右边和下边是黑的，而且每条黑边宽度不同。硬切时两块黑边一跳，看着像 PPT 转场。**合成前对每条源片做黑边检测**（全片 `cropdetect` 取交集），裁到内容再放大铺满。只检查成片不够，要逐条量源片。

### 转场：本地做，而且比你以为的要少

设计了十三处转场（钻进月饼、蛋黄变月亮、撕纸擦除、月亮对位蒙太奇、放大镜光圈），全部用 `xfade`、自定义 `expr` 擦除和静帧推拉在本地实现——不调模型、控制精确、不会漂画风。然后片主把它们**全部砍掉**，只留结尾一次推近定格。转场就该本地做：做得起，也扔得起；克制的版本往往赢。

### 音乐和字体是授权判断，不是找素材

- 平台自带的「免费音乐」（这里是抖音／剪映）通常只授权**在该平台内使用**，盖不住发到别处的片子。优先选授权无条件的曲库（这次用 Pixabay Content License），用 `curl` 下载，并在音频旁边写一份 `LICENSE.md` 记下曲名、作者、页面和授权。
- 垫乐约 -15 dB，用**以人声为键的 sidechain 压缩**自动避让，前后淡入淡出，最后整体重新归一。
- 某种书法风格的免费字体可能根本不存在：隶书这一项，GPL 的那套被公开主张抄袭且已被 Debian 下架，厂商公开授权的那套是旧式 TTC、FreeType 读不了。如实说明，退回干净的 OFL 字体（这次用霞鹜文楷），不要为了风格上一个有风险的字体。

### 同一条流水线又做了三集（2026-09-24/26）

- **片子的尾巴最容易跑偏。** 一条长推镜把竹简上的假字越推越大，最后占满画面；另一条画像石上，油灯火苗漂到了刻出来的兔子旁边。两条的前几秒都没问题。保留干净的开头，后面用它的最后一帧本地慢推接满这句旁白，不必重出。
- **跨集复用已通过的片子。** 前几集的蟾蜍镜、手册镜，在后一集里分别接了回顾句和结尾任务，省下两个任务。连着看全系列的观众会认出重复，提议时要说明。
- **本地效果要看画面，不能只看代码。** 四个本地镜头第一次都有问题：金光画在金色树干上看不见、裁切框裁到了空白夜空、推近时要的画面超出原图露出纯色底、横移幅度太小看不出在动。合成之前把每个本地镜头都抽帧看一遍。
- **旁白声音在 0.92 倍速下大约每秒 3.4 个字**，不是粗估的 5 个；有一集因此比计划长了 27 秒。先合成旁白，再定镜头长度。

## 一次 31 镜复刻试点学到的（2026-09-23/24）

一段两分钟的神话开场：31 镜，7 个反复出现的角色，3 只坐骑，2 件道具，从卡片一路做到混好音的成片。出图走了两条路线，视频用一个模型，配音用一条 TTS。哪些站得住、哪些站不住：

### 什么镜头用哪条出图路线

| 镜头内容 | 只能挂一张参考图的模型（一个 `subject_reference`） | 能挂多张参考图的模型（同框每张卡都挂） |
|---|---|---|
| 单人近景或中景 | 挂脸部特写时，通常一次就对 | 可以 |
| 空镜、道具、单独的动物 | 可以 | 可以 |
| 画面里两人以上 | **串脸**：参考图的脸渗到别人身上，年龄和服装互换 | 一次就对 |
| 人骑兽 | 坐骑变成马，人额头的记号跑到兽脸上 | 一次就对 |
| 只拍手、不露脸 | 两次都冒出一个无关的人，禁止条件不管用 | 一次就对 |

这一轮里，单参考图模型用了 64 次调用，换来 15 张卡和 18 张可用关键帧；多参考图路线 14 次调用出了 13 张关键帧，每张都是一次通过。用户的结论：**单参考图模型出的人物图，达不到拍给人看的片子的要求。** 它最多用来出空镜和道具；定妆卡，以及所有有人的关键帧，一律走能把同框所有卡都挂上的模型，并在提示词里写明不要从卡上继承什么（灰色背景、并排布局、中性站姿）。

### 卡片

- **不同景别分开出。** 在一张图里同时要全身和脸部特写，单参考图模型两次都出成三个全身。先出特写，再**挂着特写**出全身转面，最后在本地拼成一张卡。正是挂上特写，才把一张全身图的黑发修回和特写一致的白发。
- **参考图太强会被照抄。** 挂着特写出全身，结果又出了一张特写。遇到这种情况，去掉参考图，改靠文字。
- **年龄和性别漂移，加形容词解决不了。** 写明 14 岁少年，两次都出成小女孩。两次就停：要么接受这个样子（这里用传统画法里更小的孩童形象也说得通），要么换路线。

### 视频

- **排队。** 31 条傍晚提交，到第二天早上才全部完成，中间被明确的「队列已满」拒绝了 315 次，被拒不扣费。托管设 48 小时而不是 24 小时，是对的。
- **难的变形镜头一次就成**：火柱里现出人、人化成火球散开、竹简收拢成鞭、镜头下摇落到道具上。
- **4 秒下限会改变剪法。** 参考片 1 到 3 秒一切。把 4 到 5 秒的片段硬剪到那个长度，用户觉得不连贯，最后选择每条完整保留。每镜按 4 秒以上设计，或者用编排式多镜头，把快切放进一条片子*里面*。
- **台词常常开口很晚**，5 秒的片子到第 3 秒左右才说话。剪辑入点按人声段定，不要从 0 秒开始。

### 配音

- **用现成音色压音高、放慢语速来演老人、帝王和孩子，被用户判为很差。** 角色对白先生成一句小样，得到同意再生成其余的。这个账号上，服务商的声音设计和配乐接口都用不了（套餐不支持；不再对新用户开放）。
- **视频模型自带的声音是保住口型的退路**：出镜台词保留它的原声，画外台词另外解决。

### 后期

- 两个出图模型的色调不一致时，**后期统一调色**，不为对色重出图。
- 本机编码器没有文字或字幕滤镜时，**字幕和片头字卡画成透明 PNG 叠上去**。
- **有授权的曲库音乐**，要把来源页面、作者和授权写在音频文件旁边。
## 轮次隔离

第二轮不得覆盖第一轮。把路径写死成单一 `output/` 目录的脚本，会清掉之前的片段、任务记录和成片。给每一轮自己的子目录和自己的镜头定义，并验证「不带参数」时的行为与第一轮完全一致。

## 验收清单

- 素材身份已从原始内容核实，不是摘要。
- 人物设定卡与声音设定卡在每条引用它们的提示词里原样照抄，绝不转述。
- 画外音镜头写的是「禁止口型」，而不是「要求露嘴」；视频提示词里没有台词，旁白来自单独合成的音轨，不由视频模型出——除非画面里没有脸，那时台词走 `reference` 模式并钉上声音样本。
- 模型出的镜头表是当草稿逐条审过、再走普通校验保存写进项目的。
- 每个耗时步骤都显示进度；没有哪一步让操作的人猜它是不是在动。
- 长篇作品：量过篇幅和章节间隔，从文本里数过出场人物，按阶段写了角色设定，每一条都标明是原文明说还是推断并注明出处；没有把原文提交进仓库。
- 写任何镜头提示词之前先有走位：每个演员和每台机位都已摆好，每个镜头都注明用的是哪台机位。
- 根结构签名比对过；共用同一常设布景的镜头没有两个签名相同。
- 人物卡在中性灰底上出，每种状态一张；没有拿场景画面当身份参考。
- 人物压力测试跑过，看结果之前先写下通过线，并报告了结果。
- 场景描述用英文，台词用它本来的语言，舞台指示不写进台词那一段。
- 角色设定在所有镜头提示词中逐字一致。
- 生成之前先选定托管路线，并从用户自己的网络证明能用。
- 上传目录只含预期文件；凭证与元数据扫描干净。
- 每张托管的图片：HTTP 200、内容类型正确、SHA-256 与本地相符——核对一次并记下来，队列满重试时不再重新下载。
- 托管没有自带过期时，这一轮结束后删掉（或排好删除）它的预览部署。
- 生产环境与既有托管目标未被改动。
- 任务数在授权范围内。
- 每个镜头的出场表与该镜头里*看得见*的人一致，不只是有动作的人。
- 每次重试先按落盘状态分类；持有有效 `video_id` 的记录没有被重新提交。
- 前后状态对比用完全相同的命令取得，并且每次调用先确认成功，再把返回体当状态读。
- 任何当证据用的自动检测器，都已证明对已知干净的输入保持安静。
- 每条单镜头视频提示词都以「不间断长镜头」那句开头，用该提示词自己语言的资产块版本；编排段落用它的切镜计划替换那句；每条源片段都扫过内部切镜。
- 每个片段的入点和出点按它的台词窗口或动作各自选定，记在剪辑决策文件里。
- 成片：时长、分辨率、帧率符合预期，音轨存在；峰值电平查过削波；没有黑边；只在计划的边界处换镜；每段对白窗口都转写过且完整。
- 花费按实际产出记录，失败的任务记 0。
- 没有读取、回显或写入任何凭证的值，只检测存在与否。
- 每个生成物都写明归属哪个工具，且该字段在后续编辑中不会被改写。
- 音量和参考片**逐镜**比较，不只看整片；放大过的轻声录音检查过频段平衡；响度归一化实际用的模式从摘要里读过。
- 抽帧已用眼睛看过：跨时间、看整幅画面。
- 被取代的首帧、片段和任务记录归档，而不是覆盖。
- 每个无台词镜头都写了声音段落（人声槽位：无台词），并在它最响的时刻查过有没有张嘴；每个镜头的语言判定都来自人耳。
- 做任何压力测试之前，卡片组内部先一致（每个视角同一身衣服、同一头发、同样饰物）。
- 未验证的结论已记为未验证。
- 风格化片子的每个视频提示词都带锁定画风句，同一提示词语言内逐字一致；已用「首帧对比各秒」图检查。
- 全项目提交过的 `VOICE:` 段落已比对哈希，旧版已替换。
- 重新生成的台词片段全部重听，包括上一轮已通过的句子。
- 新台词已筛过叠字、句末疑问词、可避免的多音字。
- 写带版本号的产物前已确认文件名空着。
- 编排段落：切点数量与时间符合计划；画面方向从走位写了一次；每个越肩镜头写明哪侧肩膀、哪侧画框边缘；每个子镜头里每人只出现一次；锥形与口型检查逐子镜头跑过。
- 声音卡只放声音；谁说话按镜头写。
- 倍速版的声音用了 `atempo`；检查过台词结尾，不只看总时长。
- 每个自动筛查在报告结果前，都已证明对阳性对照会响、对阴性对照安静。
- 进度按持有 video id 的记录来数，不按记录文件数。
- 开头问过并记下在哪边操作（agent／控制台／两者结合）；所有项目里同一条通道同一时间只有一个调度者；有任务在跑时没重启控制台。

- 每条源片合成前做过黑边检测（cropdetect 取交集）并裁到内容。
- 旁白按静音裁头、完全不裁尾，不用语音识别的词时间做剪辑点；末字距离下一处转场约 0.9 秒。
- 旁白的音色 id、模型、语速已记录，全片同一把声音。
- 下载的音乐或字体都写了授权说明（曲名／作者／页面／条款）；只在平台内有效的授权，不用于对外发布的片子。

## 回归测试

这些测的是这份 skill，不是某部片子。下面每条规矩都是用一轮白干换来的，而且都是那种会悄悄不再被遵守的。改过本文之后走一遍；任何一次感觉又在往「堆提示词」滑回去的运行，也走一遍。

**T1 — 闸就是停。** 用户对第一道闸答得很热情（「这个方向太好了，快做」）。
*不合格：*借着这股劲一路进到提示词、出图或托管。
*合格：*只当作对第一道闸的批准，到下一道再问一次。

**T2 — 出场表从画面来。** 一个镜头是坐在某角色耳朵里的小生物的特写。
*不合格：*出场表只写一个角色，于是那个人没有角色设定就出现了，还凭空长出首饰。
*合格：*两个角色，按镜头看得见谁来定——包括它附着在谁身上。

**T3 — 反向提示词对应本镜的决定。** 一个镜头定下了一只素木盒和一卷空白卷轴。
*不合格：*只带上常设的「不要文字、不要现代物件」那段。
*合格：*逐镜的反向段落写明不要鎏金、卷轴上不要有字，因为这些是本镜决定的。

**T4 — 有些镜头不该用模型的运动。** 空白卷轴上的字卡，或一个纯风景的节拍。
*不合格：*被文字或主讲人闯进来之后，换更狠的反向提示词再生成一次。
*合格：*保留音频，在本地用已批准的首帧做推近重建视频。

**T5 — 重试前先分类。** 十八个任务回来九个失败。
*不合格：*九个全部重新提交。
*合格：*先读每条落盘记录，持有 `video_id` 的全部重新轮询，只重新提交服务端明确拒绝的那些。

**T6 — 抽样不是验证。** 被问一个片段有没有烧录字幕、嘴有没有闭上。
*不合格：*看一两帧，然后断言这个片段是怎样的。
*合格：*跨时间、扫整幅画面；否则只说样本显示了什么。

**T7 — 检测器需要阴性对照。** 一个自动筛查把每一帧都标了出来。
*不合格：*带着保留意见报告这些命中。
*合格：*先在已知干净的输入上跑一遍，安静不下来就弃用。

**T8 — 一句话不是一条规矩。** 用户说某一镜的背景显得太挤。
*不合格：*写一条永久约束禁止背景人物。
*合格：*记一条暂定倾向，只在重复出现或用户明说「从今以后」时才升格。

**T9 — 绝不代替耳朵下结论。** 包络分析显示强烈的类语音爆发。
*不合格：*报告对白正确，或语言已确认。
*合格：*报告有语音的证据，语言和内容留给用户判断。

**T10 — 失败的调用不是状态。** 一条验证命令报错，没有返回列表。
*不合格：*把空返回读成「全删干净了」。
*合格：*先确认成功再解读返回体，报警之前重新测一次。

**T11 — 连续性高于方便。** 出图流水线在一轮中途撞上额度。
*不合格：*因为另一条流水线现在可用，就用它把这一轮做完。
*合格：*等额度重置，或直白告诉用户角色会在片子中途变样。

**T13 — 无声镜头也要把声音写出来。** 一个镜头没有台词，画面里有一张脸。
*不合格：*用英文告诉模型没人说话，然后信它。
*合格：*结构化的声音段落，人声槽位写 无台词，表情固定，并在有人听之前先查最响时刻的帧有没有张嘴。

**T14 — 重试要先清掉挡住它的东西。** 提交返回 503 队列已满，退避触发。
*不合格：*重试撞进客户端自己的防重复 POST 保护，或者归档一条持有有效 `video_id` 的记录。
*合格：*只归档没有 `video_id` 且被明确拒绝的记录，然后重试。

**T15 — 弱属性不能让镜头变得不同。** 四个特写只在画面左右侧和光线冷暖上有差别。
*不合格：*接受签名比对给出的「无重复」。
*合格：*至少改掉其中一个的景别、人数或机位方向。

**T16 — 按内容裁，不从零点裁。** 一段 4.5 秒的对白片段要放进 3 秒的槽位。
*不合格：*从第一帧起 `-t 3`，然后信探测结果。
*合格：*定位台词窗口，围着它裁，再转写成片里的那一段窗口。

**T17 — 连续性参考图可能反过来压过镜头。** 一个新的大全景开场必须发生在已批准的房间里。
*不合格：*「为了连续性」把已批准的近景背面图挂上去，然后接受一张几乎照抄它的图。
*合格：*保留场景卡，去掉景别冲突的那张画面，并把景别写成占画面高度的比例。

**T18 — 整体够响不等于对上了。** 成片读数 -14.4 LUFS；参考片的开场比它响 10 dB。
*不合格：*报告整片积分值，或者把片段环境声按差值抬高然后信算术。
*合格：*逐镜比较，逐层静音找出决定电平的那一层，每个重混的版本都量一遍。

**T19 — 响度对上了，音色没对上。** 大幅提升之后开场响度对上了，但听着发嘶。
*不合格：*因为响度表全绿就算完，或者每一镜都狠切低通。
*合格：*和参考片比频段平衡，只给发嘶的镜头做低通，高频里有瞬态声音的地方保留更高的截止频率，并确认响度没有变动。

**T20 — 图片提示词的画风到不了视频。** 首帧是平涂水彩；视频提示词对画风只字不提。
*不合格：*指望首帧能撑住画风。
*合格：*每条视频提示词都带锁定画风句，用「首帧对比各秒」图核查。

**T21 — 同一处错两次就要改词。** 听的人在两轮里都听到同一个词被念坏。
*不合格：*同一句原样第三次重摇。
*合格：*绕开那个词改写，再问一次。

**T22 — 重新生成的音频就是新音频。** 只改了一句画风句；台词上一轮已通过。
*不合格：*把旧的通过沿用下来。
*合格：*请听的人再听一遍。

**T23 — 429 是拒绝，不是不确定。** 提交返回 `rate_limit_exceeded`，没有任务 id。
*不合格：*让批次停在那儿，或者升级套餐。
*合格：*归档记录，按授权数量重新提交，一次只跑一个项目。

**T24 — 已存在的标签就是被占了。** 新剪辑做好了，而 `edl-v3.json` 已经存在。
*不合格：*照样写进去。
*合格：*选下一个空着的标签。

**T25 — 结束等待的是交付物，不是进程。** 一个 CLI 写完输出文件后还要睡一分钟。驱动方在文件完整且稳定后立刻返回，远早于进程退出，并且不留下任何子进程在跑。还在增长的文件不能被误当成已完成的文件。

**T26 — 画外音镜头把口型指令反过来。** 一个走位写着旁白／背对镜头的镜头，产出的图片提示词禁止口型，视频提示词是无台词的、写明画面上的人不说话——而台词本身由单独的 TTS 音轨在合成时铺进去。
*不合格：*把台词放进一个画面里有脸的镜头的视频提示词，让视频模型在画外念出来。（画面里没有脸的画外音镜头可以在 `reference` 模式下带上台词和声音样本。）

**T27 — 草稿不能污染项目。** 一份生成的镜头表引用了不存在的场景，在应用时被拒绝并点名出错的镜头，项目文件逐字节保持不变。

**T28 — 批次覆盖的是还没开始的活。** 十二个镜头里只创建了一个任务时，「全部生成」报告十一个待创建、一个已完成——绝不报「全都已经生成了」。

**T29 — 外部路径要钉死。** 记录下来的每个 OAuth CLI 的授权文件路径，等于那个工具实际写入的路径。改名动了其中任何一个字符串，测试套件就失败。

**T30 — 编排好的切镜不是入侵。** 一个片段要求了三个定时镜头，检测器找到两处切镜。
*不合格：*把它们标成内部切镜，用「不间断长镜头」那句重新生成。
*合格：*把切点和计划边界对照——计划两处、找到两处、各在一秒以内——判为通过。

**T31 — 越肩要写明哪一侧。** 一个子镜头写着「她的肩膀虚在前景」。
*不合格：*照样发出去；模型可能把她放在两侧。
*合格：*按走位写明哪侧肩膀、哪侧画框边缘，并说明她只出现一次。

**T32 — 双人片段不照抄单人规矩。** 两张声音卡结尾都是「本镜只有他／她说话」。
*不合格：*把它们原样贴进两人都说话的片段。
*合格：*声音卡只放声音；对白段落写明谁在哪一镜说话。

**T33 — 安静的筛查要有阳性对照。** 一个字幕检测器在 112 帧里报零命中。
*不合格：*报告「没有烧录字幕」。
*合格：*在其中一帧上画上文字，发现检测器漏掉了，弃用它，改用眼睛判断。

**T34 — 两个发送方之间什么都不共享。** agent 用一次提交探队列，进去了，然后把批次的其余部分交给控制台。
*不合格：*立刻确认批次——下一次提交落在探测后几秒，吃到 429。
*合格：*等过提交间隔再交接，或者让一边发完整个批次。

**T35 — 切换共享视图是一个动作。** 人正在项目 A 跑视频轮次；agent 想看项目 B。
*不合格：*在控制台里选中 B 来读它的任务。
*合格：*直接读 B 的文件，或者先问再切。
## 常见故障

### 任务完成了却什么都没下载下来

客户端去 `metadata.url` 找素材地址，而接口把 `url` 放在响应顶层，于是成功路径反而抛错、没有保存。两个都接受：

```python
url = result.get('url') or (result.get('metadata') or {}).get('url')
```

### 生成用的 CLI 拒绝它自己的默认模型

服务端的默认模型会跑在已安装的 CLI 版本前面，报错表现为一个硬性 400，让你升级。在动手全量升级 CLI 之前，先显式钉一个较旧的模型；这类 CLI 通常在本地缓存了可用模型名单。

### 生成用的 CLI 说在等网络，网络其实是通的

出图 CLI 两次超时，日志里一直是「Reconnecting… waiting for network」，可两条网络线路都连得上服务。日志显示它连的是 `127.0.0.1:<port>`：另装的一个桥接工具改写了 CLI 的配置，把请求转给本机的中转，而这个中转没在运行。CLI 连不上时，先看日志里它实际在连哪个地址，再去怀疑网络；别的工具管理的配置，没有主人同意不要改——它可能留着一份安装记录，写着它替换掉的原值。

### 一个图片参数把提示词吞掉了

一个声明为接受多个值的 CLI 参数（`-i, --image <FILE>...`）会贪婪地把后面的位置参数提示词也当成一个文件名吃掉。这次调用于是找不到提示词，退回去读 stdin，而如果你刻意关掉了 stdin，它会在远不到一秒内退出，报错说的是 stdin 而不是那个参数。在提示词前面用 `--` 结束列表。整批瞬间失败、而不带这一个参数的调用都成功，就是这个问题的特征。

### 生成用的 CLI 在后台驱动时永远挂住

脱离终端、stdout 送到 `/dev/null`、stdin 关闭的情况下，CLI 可能无限期阻塞——进程活着、CPU 接近零，批次永远不推进，看起来像模型慢而不是挂了。驱动这类工具要用脚本，给每次调用一个明确的超时和一次重试，刻意传 `stdin=DEVNULL`，并把每次调用的输出写进各自的日志文件而不是丢掉。

**超时不等于什么都没产出。** CLI 在写完回复之前就已经把图片写下来了，所以超时可能在文件落地之后才触发。把每次超时都当失败的驱动脚本会去重试，用第二次尝试覆盖掉一张好图（2026-09-15，只因文件时间戳早于超时时刻才被发现）。超时之后先看输出是否已存在、大小是否合理，存在就保留，只在不存在时重试。而且绝不让重试覆盖已有输出——每次尝试写到各自的名字，或者跳过。

**更好的做法：文件写完就停止等待，而不是等进程结束。** 等退出不只是慢，看上去还像坏了。一张角色卡大约一分钟就落盘了；CLI 接着又花了**九分钟**写它的收尾总结（405 KB 的日志），控制台一直停在「generating」。用户断定它卡住了，又按了一次生成，结果在它后面排了一个重复任务。交付物才是契约，所以要轮询它：一旦文件*完整*且连续几秒不再增长，就结束进程。完整指结构上完整，而不只是非空——PNG 带 `IEND` 块，JSON 能解析。超时留着做兜底，并确保每条退出路径都杀掉子进程，免得漏掉的 CLI 之后白烧配额。

### 字幕被烧进画面

一旦要求对白，模型就会加字幕，而且有的镜头有、有的没有。不管交付走哪种方式，模型自己加的字幕都是错误的来源：风格、位置、措辞都不一致，而且不重生成镜头就改不了。

所以无论如何都要在每条提示词里禁止文字、字幕和水印，然后在组装环节用你手里已有的脚本自己加字幕。要么每个镜头都烧、用同一种样式，要么一个都不烧。

找模型自带文字时有两个坑。它不只给**台词**配字幕——还会把提示词里的**舞台指示**渲染出来，把「pleased with himself, warming up as he goes」这类语气指示当成对白烧进字幕带。而且文字不只出现在那条带上：它可能以道具上的文字（一卷书、一幅悬挂的横幅）出现在画面中部，也可能在镜头中途淡入而不是从第 0 帧就有。所以每个片段要在多个时间点抽样，看整帧而不是只看下三分之一的一帧；底部一条模糊够不到画面中间的道具。文字侵入盖住了片头字卡时，保留片段的音频，把它的视频重建成对已批准关键帧的缓慢推进——不用新开计费任务，字卡完全按设计出来。

### 组装脚本默认每个镜头一样长

一版镜头全是 5 秒的剪辑，藏着一堆写死的 5 秒：音频裁剪、`-t` 上限、总时长断言、淡出起点、审片帧的偏移。下一轮 6–12 秒混合镜头进来，每个更长的片段就被静默截断——一段 10 秒的旁白丢掉后半——而且照样产出一个看起来合理的文件。在有变长镜头的一轮之前，所有依赖时长的值都要从该片段自己探测出的长度推导。

### 每个片段都从第一帧开始裁

生成长度是整秒，剪辑想要 3.5 或 6.5，于是最直接的组装就是把每个片段从第 0 帧裁到剪辑长度。它会静默地切掉对白：一个 4.5 秒片段里模型从 2.56 秒才开口，裁到 3 秒整句就没了，成片里只剩一个字（2026-09-14）。没有任何东西报警——文件探测正常，而前期的 TTS 时长门槛对*视频模型*选择何时开口一无所知。

**默认用整段片段，只在有东西逼着裁时才裁。** 用户判定 4–5 秒片段裁成 1–3 秒显得零碎，选择每段都用整段（2026-09-24）；只在内部切换、尾部漂移或固定槽位（一句旁白、一张字卡）逼着裁时才裁。**然后从片段的内容里定入点和出点。** 对白片段要找出说话的时间窗（静音检测给出有声段；本地语音识别的逐词时间戳给出顺序——它们能定位台词，但可能偏早，所以剪点落在它们外面一拍，绝不落在上面），从它前面一拍开始、后面一拍结束。动作片段要沿时间看多帧，保留动作读得清、没有内部切换闯入的那一段。把每镜选定的点连同一行原因记进剪辑决策文件，重剪就只是改数据。画面必须在台词结束前切走时，让对白音频领先或拖尾于画面（J 剪和 L 剪）。然后验证成片而不是片段：对成片的每个对白时间窗做转写，检查台词完整，并按脚本、按实测语音时刻烧字幕。

静音检测出的时间段不等于一句话。一句带自然停顿的台词——「这么漂亮的匣子，谁看了不心动？」在「匣子」后停了 0.8 秒——会被切成两个有声段，按第一段的结尾剪，成片就只剩半句（2026-09-16）。台词范围要用片段逐词时间戳的转写结果从第一个字取到最后一个字，或把转写覆盖到的有声段合并，然后对成片做一次转写——这次就是靠这一步发现的。

### 模型在片段内部切镜

在每个**源片段**上跑镜头切换检测（对缩小后的副本用 `select='gt(scene,0.25)'`）能找出模型加的切换；在**成片**上跑，每个命中都应落在计划好的镜头边界上，其余的都是闯入。在有内部切换的片段里，切换之后的段落都带着连续性损伤——服装变了、多出一个人、换了地点——所以可用部分几乎总在第一次切换之前。裁到那里，剩下的太短就用「不间断单镜」措辞重生成。夜晚的暗场在这个阈值下可能把真切换藏住；暗场素材要调低阈值，并肉眼确认。

对**有脚本**的序列，同一个检测器就成了验收测试：命中数量必须与计划边界一致，位置落在边界大约一秒之内。少一个切换意味着两个镜头合并了；多一个就是老问题的闯入。

### 成片里对白太轻

生成的语音可能低到 -35 dBFS 附近。在逐片段的处理环节做响度归一化可以提上来。之后要检查成片的峰值电平，因为拼接时的重编码可能把峰值推到满刻度而削波。

### 对只有环境声的镜头做响度归一化

以对白为目标的 `loudnorm` 对有台词的镜头是对的，对其他一切都是错的。用在只有风声或只有水声的节拍上，环境声会跟语音一样响；用在本地重建、声底从一个安静时间窗（峰值约 -73 dBFS）里取出的镜头上，它把噪声底抬了五十多 dB，交付的是嘶声。用镜头自己的脚本条目来决定：有台词用 `loudnorm`，没有台词用温和的带限制器的增益。

### 按镜头对齐参考片的音量

整片响度达标不等于每一镜都对。一版成片整片 -14.4 LUFS、和参考片差不多，开头三镜却比参考片轻 10 dB（2026-09-15）。只有**逐镜**比较才看得出来：瞬时响度按 0.25 秒分箱，每镜**按能量平均而不是按分贝平均**，再和参考片同一镜的时间段对比。

修得准不准看四点：

- **先找出是哪一层声音撑着音量，再决定抬谁。** 一次静音一条轨，重建混音后测量。那段开头里合成的风声比片段自带环境声高约 6 dB，只把环境声抬 8 dB，镜头只响了约 1 dB。要抬占主导的那层，或者两层一起抬。
- **测重建后的混音，不要靠算。** `loudnorm` 设了 `linear=true`，线性增益会顶破真峰值上限时会悄悄退回动态模式（那一版每次构建都退了），而动态模式对安静段的提升是非线性的：开头抬 8–12 dB 响了 12 dB，再多抬 2 dB 毫无变化。看摘要里的 `Normalization Type`，用几秒钟的只重建声音来迭代，不要每次全片渲染。
- **录得轻的声音放大后会带出嘶声。** 环境声放大 18 dB 后音量对上了，但声音发亮：5 kHz 以上比中频只低 12.7 dB，参考片低 20.7 dB。逐镜加低通，响度基本不动，降到 18.1 dB。截止频率按内容定：只有底噪的房间声用 2 kHz，瓷器碰声这类高频瞬态所在的镜头留到 4 kHz。
- **证明混音代码改动不影响声音，要靠对比，不靠校验和。** 噪声发生器没有固定种子，每次构建都不同。用新代码构建两次：若两次之间的差异和改前改后的差异一样（-31.3 dB 对 -31.2 dB），改动就是中性的。更好的做法是给每个噪声发生器固定种子（`anoisesrc=…:seed=N`），同一剪辑单重建逐字节一致，校验和就够用。
- **有意识地选响度统一方式。** 普通增益加峰值限制器，混音里改 +N dB 成片就是 +N dB；`loudnorm` 退回的动态模式不是。已有版本在两种方式间切换会让安静段变化——同一套设置换成线性增益后开头轻了 3–6 dB——所以旧版本保持调音时用的方式，每次渲染后记录实际用的方式。

混音设置（轨道与单个声音的音量、静音、压低配乐、目标响度）作为数据存进每个剪辑版本，每次修改都另存新版本。浏览器里的混音试听只是近似，还原不了侧链压缩和最终的响度统一，定稿前要拿渲染出的成片和参考片再对一遍。

### 多出来或少掉的话，以及撞在一起的人声

一个镜头的音频里可能出现比脚本更多的发声段。数一数包络里的爆发次数，把差异指出来，让人收听后判断。

更糟的一种形式是**两个声音重叠**——半秒钟的语音压在另一段语音上，听众听到的是糊成一团、听不清，而不是多出一句话。它是在一段看起来还算正常的片段上靠耳朵发现的（2026-09-10），就在模型开始念舞台指示的前一刻。

**没有一个包络指标经得住测试。** 最明显的候选——异常长的不间断有声段，因为重叠的语音在句与句之间从不回落到底噪——以 50 毫秒分辨率对照用户已判定合格的片段测过，结果合格片段反而*更差*（3.5 秒不间断有声对 2.05 秒）。按检测器那条规则，它被弃用了，而不是带着附注上报。爆发次数仍值得标出；重叠似乎无法用这种方式检出，说能检出就是在凭空制造把握。这是「耳朵的裁决不可省略」最尖锐的例证。

### 反复出镜的讲述者变得僵硬

一个在十个镜头里出现的讲述者是观众目光所在，如果每张关键帧都把他放在同一个胸部以上四分之三侧的角度、同样的半笑、同样的单手摊开手势，不管他身后的故事怎么变，序列读起来都是静止的。在他的各个镜头之间刻意变化景别和手势——更近和更远、单手和双手、回指场景和面向镜头——正如其他任何反复出现的机位都需要构图变化。把它设计进每镜的文本里；设定集固定的是他是谁，不是每镜怎么取景。

### 空白的表面是在邀请写字

一卷书、一幅横幅、一块招牌，刻意留空准备自己加标题——那正是模型会写字的地方，不管提示词禁得多坚决，而且它编出来的字常常是畸形的。同一个标题镜头连续两轮都出了这事。要么把这类表面挪出画面，要么从一开始就计划**那里不用模型的运动**：保留片段的音频，把视频重建成对已批准关键帧的缓慢推进，再合成你自己的标题。便宜、精确，而且不多花一个任务。

### 空荡的画面是在邀请填人

空白表面陷阱的孪生兄弟。纯风景镜头——没有角色、没有要对口型的东西——正是模型会发明一个**主持人**的地方：照片级真实的人，居中站在画面里，对着镜头比划着说话。同一个镜头连续两次出现（2026-09-09/10）。第一次出了一个现代健身网红，穿印着品牌的运动内衣，别着领夹麦，戴着智能手表；重试时提示词加固到点名禁止人、现代服装、logo、麦克风和手表，出来的是同样的构图，只是换成了古装。禁止*属性*没有用，因为吸引它的是**构图**，不是服装。

有一个诱因不管怎样都值得修：无对白条款写成「every character keeps their mouth shut」提到了角色，而一个没有角色的镜头把它读成了补一个角色的许可。否定条款要从镜头自己的演员表生成——演员表为空时，就说画面里没有任何活物，而且永远不要用「character」这个词。

但别指望这就能管住。可靠的对策和空白表面陷阱一样：**那个镜头不用模型的运动**。在本地把它重建成对已批准关键帧的缓慢推进或摇移（对放大后的静帧做 `zoompan`），这正是风景节拍本来就想要的，不花计费任务，得到的恰好是被批准的那一帧。环境声从被弃用片段最安静的时间窗里取，而不是用它的整条音轨——那里面有被发明出来的主持人的声音。

### 本地推进镜头粘在左上角

在本地做推进镜头，逐帧放大再裁一个固定的 1280×720 窗口，裁剪偏移写成 `in_w - 1280` 的比例，看起来对，其实不对：FFmpeg 的 `crop` 从第一帧取 `in_w`/`in_h`，于是放大后的帧越来越大，偏移却停在零附近，每次推进都往左上角漂。两次用不同水平中心的测试渲染出来完全一样（2026-09-15），这就是征兆。溢出量要从缩放表达式本身算——和 scale 用的同一个表达式——再乘以中心比例。放大到覆盖时，也要真正铺满画面而不是补边：一个 1280×704 的源补到 720，交付出去上下各带 8 像素黑边。

### 无对白镜头自己编了一句话

空荡画面在音频上的对应物。靠人耳听出的症状来识别（2026-09-11）：没有脚本台词的镜头在说话，其中几个说的是**同一句话**，与故事无关，也不在任何提示词里。互不相关的图片出现相同内容不可能来自图片；这是模型用一个默认值填了一个没分配的音频槽位。诊断和修好它的措辞在门槛 2。两件没用的事：英文「no one speaks / no narration / no voice-over」（它点名了要禁的东西），以及一段去掉所有语音词、给了正面任务的英文音轨描述（嘴照样张开）。

### 一次失败的调用被当成了状态变化

用来证明什么都没损坏的验证，本身能制造出一场灾难。部署后列出托管频道，在一个没有配置文件的目录里运行，返回的是 `{"status": "error", ...}` 而不是频道列表；比较脚本把缺失的列表读成*零个频道*，报告说包括生产在内的所有频道都被删了。什么都没发生。

这个形状不限于托管：一旦你去索引响应体，空结果和失败调用就分不开了。**先断言调用成功，再把它的内容当状态解读**，前后快照要用逐字节相同的命令、参数和工作目录——两次不同的调用之间的比较不是比较。骇人的差异先当作可疑的测量，直到干净地重新测过一次为止；这是「抓取来的摘要在基本事实上也可能错」的姐妹规则。

### 什么都能命中的检测器不是证据

在抽样帧上跑 OCR 找烧进去的字幕，100% 的帧都命中——在照片级画面上用 `--psm 11`，从岩石纹理、羽毛和枯草里读出几百个垃圾字形。没有负例的筛查没有区分力，报告它的输出就是打扮成勤勉的误报。

信任任何自动检测器之前，先在**已知干净**的帧上跑，确认它不叫。不安静就弃用并说明，而不是带着附注上报它的输出。烧入文字的可靠方法仍然是目视：从每个抽样帧裁出字幕带，把裁片拼成一张图，然后看——拼图让 72 帧变成一眼而不是七十二眼，而整帧仍要单独过一遍，找道具上的文字。

镜像的错误同样容易犯：把命中解释掉。一轮里六个无对白镜头全部标为疑似语音，它们恰好都有一架钟，于是有人提出「敲钟在这个指标下读起来像一个字」是可能的原因。这是错的——六个里有五个真的在说话。对一整类都叫的检测器不能支持任何一种结论。去拿它给不了的证据——最响时刻的帧，看嘴有没有张开——其余的交给耳朵。

### 语音识别在近乎无声时会胡编

对无台词片段跑 `whisper-large-v3-turbo`，会得到「请不吝点赞 订阅 转发…」「字幕志愿者 …」或一长串同一个字（「明明明…」「好好好…」）——这是训练数据里字幕的残影，不是语音。响度在 -40 LUFS 左右或更低的片段，转写一律当噪声，改为在声音最响的时刻看嘴。

### 抽一两帧不算验证

嘴是否一直闭着、是否出现文字、角色是否漂移——这些是*每一帧*的属性，抽一两帧建立不了。一个片段可以开头干净、中途变样。只报告样本显示了什么（「我检查的那几帧显示 X」），绝不报告片段是什么样，除非你沿时间扫过。这个失败是自相似的：在烧入字幕上学过一次之后，仍然很容易每个片段只抽一帧看嘴，然后用同样的虚假把握下结论。

### 在听不见的情况下证明「有语音」

你无法直接验证音频，但可以拿到真实证据。计算每 100 毫秒的 RMS 包络：低底噪之上出现离散爆发、动态范围大致超过 20 dB，指向语音；纯环境音通常在 12 dB 以内。然后在这些爆发的时间点抽帧，检查口型在帧之间是否真的变化。把这个当作有语音的证据来报告，绝不当作语种或内容的确认。

### 署名不等于归属

生成物上写着「最后由谁编辑」，流程拿它判断该用哪个渲染器；而另存新版本会改写这个字段，于是通用剪辑单被交给片子专用脚本，崩在它要求的字段上（2026-09-17）。署名、编辑者、时间戳描述的是「最后动过谁的手」，不是契约。下游要知道文件归谁管，就单独写一个字段、改动时保留，兜底用结构判断（文件带不带那个工具要求的字段），不要用文本署名。

### 相对路径算错了基准

生成的剪辑单按「通常写到的目录」算片段路径，实际写到别处，渲染报找不到素材；同一个形状还出现在「脚本路径按项目根目录解析，而工具实际在子目录里运行」。相对路径要按文件真正落地的目录算，配置里的路径要按工具真正的工作目录解析，并在运行前断言目标存在，而不是在运行中。

### 出图账号在批次中途用完配额

按个人套餐计费的生成 CLI 可能在一批任务跑到一半时耗尽额度：调用开始在几秒内失败，用量上限的提示里写着重置时间，而同一轮里之前的调用都成功了。这不是额外扣费，也不是提示词问题——不要循环重试，也不要开始改写提示词。报告重置时间，买不买额度留给账号的主人决定。

### 退回到供应商网页版出图

CLI 配额耗尽时，同一家供应商的聊天网页在已登录的浏览器里仍能出图——但它是**另一条管线，而且看得出来**。驱动它学到三件值得留下的事：

- **绝不要把长提示词打进输入框，也别指望粘贴。** 浏览器自动化扩展可能提供 `type` 却不提供 `cmd+v`、`cmd+a` 或 `Delete`，留下一段打了一半、清不掉的草稿。更糟的是，手工重打一段长的非 ASCII 提示词会把它打坏——单个错字混进去，设定集就不再逐字节一致，而这正是它存在的唯一意义。**直接附上提示词文件**，让模型去读；保真度就不花任何代价。
- **单靠一张人像参考图复现不出既定角色。** 用同一张照片去要，网页版返回的是另一个人（头发不同、脸不同），和 CLI 整轮产出的那个不一样。把一张已批准的帧作为外观参考喂给它，大部分拉回来了——但服装多出了设定集里说是素面的刺绣，渲染也比其他部分的扁平绘本感更光亮、更立体。
- 所以把轮中切换管线当作**连续性断裂，而不是图方便**。这一轮已有一条路径产出的已批准帧，就在那条路径上做完；等配额重置比一个袍子在剪辑中途变样的角色便宜。

### macOS 上没有 `timeout`

macOS 不是 coreutils 环境。用你自己 harness 的调用超时，而不是去包裹命令。

### 残留的 `.git/index.lock`

一次被中断的 git 写操作留下的 0 字节锁文件，会静默地挡住之后所有的 git 操作，只要它还在。确认它是 0 字节、时间戳很旧、且没有 `git` 进程在跑，然后删掉它。

### 视频文件进不了 git

很多仓库刻意排除 `*.mp4` 之类来阻止仓库膨胀，而这种排除很容易被误认为是疏漏。先弄清楚这条规则是不是有意为之，把冲突讲出来，而不是强行 add。大媒体通常该进备份或对象存储，不该进 git 历史——历史事后削不掉，除非重写。
